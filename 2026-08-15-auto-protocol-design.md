# 자동 협업 프로토콜 설계 — Claude Code ↔ 할매봇

질문: 대화가 끝나면 서로 자동으로 트리거하고, 할매봇은 사용자에게 **승인 요청과 보고만** 하도록 만들 수 있는가?
작성일: 2026-08-15 · 기준 `46c626a`

---

## 결론

**절반은 이미 있다. 한 조각이 완전히 비어 있다.**

| 필요한 것 | 상태 |
|---|---|
| 메시지 큐 (지시 전달·중복 방지) | ✅ **이미 있음** — `agent_messages` + `pending→claimed→read` |
| 폴링/픽업 도구 | ✅ **이미 있음** — `agent.tasks.list_pending` / `claim` / `report` |
| 즉시 전달 | ✅ **이미 있음** — WS push, 미연결 시 DB 폴백 |
| 승인 기록 | ✅ **이미 있음** — `POST/GET /api/approvals` |
| 할매봇의 주소 | ❌ **없음** — 에이전트 미등록 (해결 쉬움) |
| 나를 깨우는 방법 | ⚠️ **조건부** — 스케줄 루틴 필요 |
| **사용자에게 도달하는 알림** | ❌ **없음 — 이게 핵심 공백** |

---

## 1. ✅ 프로토콜은 이미 설계돼 있다

이 시스템은 애초에 에이전트 간 협업용으로 만들어졌다. 새로 만들 게 별로 없다.

```
보내는 쪽                     받는 쪽
agent.send_message  ──▶  agent_messages (status='pending')
                                │
                         WS 연결돼 있으면 즉시 push
                         아니면 DB 에 남아 폴링 대기
                                ▼
                         agent.tasks.list_pending
                         agent.tasks.claim      ← 중복 처리 방지 (30초 리스)
                         (작업 수행)
                         agent.report           ← trace_id 유지
```

`claim`이 있다는 게 중요하다. 나와 할매봇이 동시에 같은 지시를 집어드는 사고를 막아준다.

---

## 2. ❌ 할매봇에게 주소가 없다 (해결 쉬움)

등록 에이전트 16명 중 할매봇이 없다. 그래서 `agent.send_message(to_agent=...)`에 넣을 값이 없고,
지금은 **사용자가 손으로 지시서를 전달**하고 있다.

**조치:**

```bash
# 1) 에이전트 등록 (owner 로 담당자 표시 — 이번에 owner 지원 추가됨)
curl -X POST .../api/agents -H "Authorization: Bearer <ADMIN>" \
  -H "Content-Type: application/json" \
  -d '{"id":"ag_hermes","name":"할매봇","role":"VPS 배포·운영",
       "color":"#7c8db5","owner":"<담당자>"}'

# 2) 키 발급 (mcp:read + mcp:execute — admin 은 주지 말 것)
curl -X POST .../api/agents/ag_hermes/credentials -H "Authorization: Bearer <ADMIN>" \
  -H "Content-Type: application/json" \
  -d '{"name":"hermes-vps","scopes":["mcp:read","mcp:execute"]}'
```

> **할매봇에게 `mcp:admin`을 주지 말 것.** 자격증명 발급 권한까지 넘어간다.
> 배포·실행에는 `mcp:execute` 로 충분하다.

---

## 3. ⚠️ 나를 깨우는 방법 — 자동은 되지만 조건이 있다

**나는 스스로 깨어나지 못한다.** Claude Code 세션은 사용자가 부를 때 실행된다.
할매봇이 `agent.send_message`를 보내도, 내 세션이 떠 있지 않으면 아무도 안 읽는다.

가능한 방법 둘:

| 방법 | 동작 | 한계 |
|---|---|---|
| **스케줄 루틴** (`/schedule`) | cron 으로 세션이 깨어나 `agent.tasks.list_pending` 확인 → 처리 | 주기만큼 지연. 비용 발생 |
| **`/loop`** | 세션이 켜져 있는 동안 주기적으로 확인 | 세션이 닫히면 멈춤 |

> 즉 "대화가 끝나면 서로 트리거"는 **엄밀히는 불가능**하고,
> **"할매봇이 큐에 남기면 다음 폴링 때 내가 집어든다"** 가 정확한 표현이다.
> 실용적으로는 같은 결과지만, 지연이 생긴다는 점은 알고 있어야 한다.

**할매봇 쪽은 내가 모른다.** VPS에서 어떻게 기동되는지(cron? 상주 프로세스? 수동?)에 따라
설계가 달라진다 — **이건 알려주셔야 한다.**

---

## 4. ❌ 핵심 공백 — 사용자에게 도달하는 알림이 없다

질문의 목표는 "할매봇이 **나에게** 승인요청과 보고만 하는 것"인데, **바로 이 경로가 없다.**

확인한 사실:

- `POST /api/approvals` 는 **기록만 한다.** `decision` 기본값이 `'pending'` 이고,
  누구에게도 알리지 않는다. 사용자가 직접 `GET /api/approvals` 를 봐야 안다
- **텔레그램은 구현돼 있지 않다.** `agent_orchestrator.py:237` 은
  `result["message"] = f"[보고] {label} 완료"` — 문자열만 만들고 **전송하지 않는다**.
  `server.js:977` 은 향후 확장 주석일 뿐이다
- `ag_communicator` 의 `tools: ["telegram","api"]` 는 **선언만 된 상태**
  (`PLANNED_TOOLS` 에 등록해 둔 그것)

> 이번 세션에서 **다섯 번째** 만나는 같은 패턴이다 — 알림 기능이 스펙·역할표·툴 목록에는 있고
> 실제 전송 코드는 없다.

**즉 지금 구조로 만들면, 할매봇이 승인을 요청해도 사용자는 그걸 모른다.**
사용자가 주기적으로 웹 UI나 API를 확인해야 하는데, 그건 "보고만 받는다"는 목표와 반대다.

### 선택지

| 방안 | 구현량 | 특징 |
|---|---|---|
| **텔레그램 봇** | 중 | 봇 토큰 + `sendMessage` 호출. 휴대폰으로 즉시 도달. 가장 목표에 맞음 |
| **이메일** | 중 | SMTP 설정 필요. 즉시성 낮음 |
| **웹 UI 알림함** | 소 | `GET /api/approvals` 를 띄우는 패널. **여전히 사용자가 봐야 한다** |
| **GitHub Issue** | 소 | 이미 저장소가 있어 추가 인프라 없음. 알림은 GitHub 앱이 대신해줌 |

> **텔레그램이 목표에 가장 부합한다.** 이미 역할 설계(`ag_communicator`)가 그걸 전제로 돼 있고,
> 승인 요청에 인라인 버튼(승인/거부)까지 붙일 수 있어 왕복이 한 번에 끝난다.

---

## 5. 그래서 만들 수 있는 최종 그림

```
        ┌──────────────── 사용자 ────────────────┐
        │  승인 요청 / 완료 보고만 받는다 (텔레그램)  │
        └───────▲───────────────────────┬────────┘
                │ 알림                   │ 승인/거부
                │                       ▼
        ┌───────┴───────────────────────────────┐
        │        /api/approvals  (기록)          │
        └───────▲───────────────────────────────┘
                │
   ┌────────────┴────────────┐
   │                         │
[Claude Code]  ◀── 큐 ──▶  [할매봇 ag_hermes]
 코드 작성·검토              VPS 배포·운영
 git push                   git pull + pm2
   │                         │
   └──── agent_messages ─────┘
      pending → claimed → read
```

**역할 분담이 자연스럽게 나뉜다:**

- 나 — 코드 작성·검토·지시서 생성 → git push → `agent.send_message(ag_hermes, 'command')`
- 할매봇 — `claim` → `git pull` → 테스트 → 배포 → `agent.report`
- **승인이 필요한 지점**(프로덕션 쓰기, 롤백, 스키마 변경)에서만 `/api/approvals` + 텔레그램
- 사용자 — 알림 받고 승인/거부. 그 외에는 개입 없음

---

## 6. 구현 순서

| # | 작업 | 규모 |
|---|---|---|
| 1 | `ag_hermes` 에이전트 등록 + 키 발급 (`mcp:execute`) | 소 |
| 2 | **텔레그램 알림 구현** — 봇 토큰, `sendMessage`, 승인 인라인 버튼 | **중 (핵심)** |
| 3 | 승인 게이트 정의 — 어떤 작업이 승인 대상인가 | 소 (설계) |
| 4 | 할매봇의 폴링 루프 (`list_pending` → `claim` → 수행 → `report`) | 중 |
| 5 | 내 쪽 스케줄 루틴 — 큐 확인 주기 결정 | 소 |

**2번이 없으면 나머지를 다 만들어도 목표가 달성되지 않는다.** 여기부터 하는 게 맞다.

---

## 7. 알아야 하는 것 (사용자 확인 필요)

1. **할매봇은 VPS에서 어떻게 기동되나?** cron / 상주 프로세스 / 수동 실행?
   → 4번 설계가 여기 달려 있다
2. **텔레그램을 쓸 수 있나?** 봇 토큰 발급이 가능한지, 아니면 다른 채널을 원하는지
3. **승인 대상의 범위** — 프로덕션 쓰기만? 배포도? 코드 수정은 자동 통과?

---

## 알려진 제약

- `agent.send_message` 스키마에는 **본문 필드가 없다.** `payload_ref` 만 받는다.
  지시서 같은 긴 내용은 계속 **git 으로 전달**하고, 메시지는 "무엇을 하라 + 어느 커밋" 정도의
  포인터로 쓰는 게 맞다. (지금 방식과 동일하되 자동화되는 것)
- 폴링 주기만큼 **지연**이 생긴다. 즉시성이 필요하면 WS 상주 연결이 필요하다
- 자동화 범위가 넓어질수록 **잘못된 지시가 자동 실행될 위험**도 커진다.
  승인 게이트(3번)를 좁게 잡지 말 것
