# 커멘드센터 프론트엔드 검토 — 수정사항 · 개선 · 고도화 전략

대상: https://joker8awesome.github.io/workflow-builder/
작성일: 2026-08-15 · 기준 커밋 `9ff6266`
방법: 배포본 실측 + 로컬 소스(`index.html` 5,472줄 · `sw.js`) 대조 + 백엔드 CORS/인증 동작 확인

> 이 리포트의 모든 수치는 **직접 확인한 값**이다. 스펙 문서의 미검증 주장은 인용하지 않았다.

---

## 요약

배포본은 로컬 `index.html`과 **동일하다** (CRLF 차이 제외, gzip 후 67KB). 페이지 자체는 건강하다.

문제는 페이지가 아니라 **연결**이다. 백엔드는 이 페이지를 받아들일 준비가 끝나 있는데
프론트엔드가 백엔드를 부르지 않는다. 그리고 지금 그냥 연결하면 **프로덕션이 공개 쓰기 상태가 된다.**

| 구분 | 항목 | 심각도 |
|---|---|---|
| 수정 | A. 배포본이 백엔드에 닿지 못함 + 연결 시 무인증 쓰기 노출 | 🔴 구조 |
| 개선 | B. SW가 쓰지 않는 폰트 1.9MB를 캐시 | 🟠 |
| 개선 | C. 접근성이 스펙 주장과 어긋남 | 🟠 |
| 개선 | D. manifest 동적 생성 — JS 실패 시 PWA 불가 | 🟡 |
| — | E. XSS — **검증 결과 문제 없음** | ✅ |

---

## A. 🔴 배포본이 백엔드에 닿지 못한다 — 그런데 지금 연결하면 안 된다

### 현상

`index.html:1040`:

```js
const API_BASE = (window.__WF_API__ ||
  (location.hostname === 'localhost' || location.hostname === '127.0.0.1'
    ? 'http://localhost:3737' : ''));
```

GitHub Pages에서는 `location.hostname`이 `joker8awesome.github.io`이므로 **`API_BASE = ''`**.
모든 호출이 상대 경로가 되어 `https://joker8awesome.github.io/api/workflows` → 404.
`checkServer()`가 실패를 잡아 localStorage 모드로 떨어진다. **즉 공개 사이트는 영구히 단독 모드다.**

### 그런데 백엔드는 이미 준비돼 있다 (실측)

```
$ curl -H "Origin: https://joker8awesome.github.io" .../api/workflows
HTTP/1.1 200 OK
Access-Control-Allow-Origin: https://joker8awesome.github.io
```

`server.js:86`의 기본 허용 오리진에 **`https://joker8awesome.github.io`가 명시**돼 있다.
서버는 Pages를 받아들이도록 설정돼 있는데, **프론트가 그 주소를 부르지 않는다.**
→ 이 CORS 설정은 **일어나지 않는 연결을 위한 죽은 설정**이다.

### ⚠️ 그래서 "한 줄 고치면 된다"가 아니다

`API_BASE`를 VPS로 바꾸는 건 한 줄이지만, **지금 하면 위험하다.**

`server.js:101`:
```js
function requireAuth(req, res, next) {
  if (!ACCESS_TOKEN) return next();   // 미설정 시 인증 없이 통과
```

`WF_ACCESS_TOKEN`은 **미설정으로 확인됐다** (할매봇 STEP 1 보고).
`POST/PUT/DELETE /api/workflows`에 `requireAuth`가 걸려 있지만 토큰이 없으면 그냥 통과한다.

**공개 URL을 프로덕션 DB에 연결하면, 방문자 누구나 워크플로우를 생성·수정·삭제할 수 있게 된다.**

### 순서 (반드시 이 순서로)

```
1) WF_ACCESS_TOKEN 설정 + pm2 재시작
2) 토큰 전달 방식 결정   ← 여기가 실제 설계 결정
3) 그 다음에 API_BASE 연결
```

### 2)가 어려운 이유 — 정적 페이지는 비밀을 담을 곳이 없다

토큰을 `index.html`에 넣으면 공개된다. 선택지는 셋이고, **각각 다른 제품을 뜻한다:**

| 방안 | 동작 | 대가 |
|---|---|---|
| **공개 읽기 전용** | Pages는 `GET`만. 쓰기는 로컬/VPS UI에서 | 가장 안전·간단. 공개 사이트는 뷰어가 됨 |
| **사용자별 키 입력** | 방문자가 자기 `wf_ak_` 키를 입력, `localStorage` 보관 | 팀원만 쓰는 전제. 키 발급 UI 필요 |
| **프록시 도입** | Pages → 인증 프록시 → VPS | 가장 유연하나 구성요소 추가 |

> **이건 제가 정할 문제가 아닙니다.** "공개 사이트를 무엇으로 만들 것인가"에 대한 답이
> 곧 방안 선택입니다. 데모/포트폴리오면 1번, 팀 도구면 2번입니다.

**연결 전까지는 현재 상태(localStorage 단독)가 오히려 안전합니다.** 급하지 않습니다.

---

## B. 🟠 서비스워커가 쓰지 않는 폰트 1.9MB를 캐시한다

`sw.js:3`:

```js
const ASSETS = [
  './index.html',
  './fonts/Isamanru-Light.woff',    // 633,516 B
  './fonts/Isamanru-Medium.woff',   // 623,280 B
  './fonts/Isamanru-Bold.woff',     // 667,068 B
];
```

그런데 커밋 `451facf`("Isamanru font glyph corruption — switch to system Korean fonts") 이후
**`index.html`의 Isamanru 참조는 0회**다. 실제 폰트 선언은 `ui-monospace, monospace` / `inherit` 뿐이다.

| 항목 | 크기 |
|---|---|
| 페이지 (gzip 전송) | **67 KB** |
| SW가 추가로 받는 폰트 | **1.9 MB** |
| 비율 | 약 **29배** |

**첫 방문 시 쓰지도 않는 폰트 1.9MB를 내려받는다.**

> 검증 메모: 처음엔 `cache.addAll()`이 원자적이라 폰트 404 시 **SW 설치 자체가 실패**할 것으로 의심했으나,
> 세 파일 모두 `200`으로 서빙됨을 확인해 그 가설은 **폐기**했다. 설치는 정상이고, 문제는 낭비뿐이다.

**조치:** `ASSETS`에서 폰트 3줄 제거 + `CACHE` 버전을 `v4`로 올림(기존 캐시 무효화).
저장소의 `fonts/` 1.9MB도 다른 곳에서 안 쓰면 함께 정리.

---

## C. 🟠 접근성 — 스펙이 완료라 적었지만 마크업에 없다

실측:

| 신호 | 수 |
|---|---|
| `<button>` | **108** |
| `aria-*` | **1** |
| `role=` | **1** |
| `tabindex` | **1** |
| 인라인 `onclick` | 8 |

`<html lang="ko">`는 올바르게 설정돼 있다.

스펙 §6은 **"14차 사용자 UX(온보딩/접근성)"를 완료로 표시**한다. 마크업은 그렇지 않다.

> 이건 이번 세션에서 세 번째로 만나는 **동일 패턴**이다 —
> `agent.list`의 `capability` 필터, `online` 지표, 그리고 접근성.
> 전부 **스펙·스키마·UI에는 있다고 적혀 있고 코드에는 없었다.**
> 로드맵 Phase 1-2(계약 테스트)를 최우선에 둔 이유가 이것이다.

**조치 (효과 순):** 아이콘 전용 버튼에 `aria-label` → 모달에 `role="dialog"` + 포커스 트랩 →
캔버스 노드 키보드 이동 → 토스트 `aria-live="polite"`.

---

## D. 🟡 manifest가 동적 생성이라 JS 실패 시 PWA 설치가 안 된다

```
manifest.json        404
manifest.webmanifest 404
site.webmanifest     404
```

정적 파일이 없고 JS로 동적 생성한다("manifest 동적 생성" 주석). 동작은 하지만,
JS 실행 실패·CSP 제한 환경에서는 설치 배너가 뜨지 않는다. 정적 `manifest.webmanifest` 한 개를
두고 `<link rel="manifest">`로 거는 편이 견고하다.

---

## E. ✅ XSS — 검증 결과 문제 없음

`innerHTML` 54회 / `escapeHtml` 79회라는 비율은 근거가 못 되므로, **보간이 들어가는 지점만** 추적했다.

`innerHTML =` 에 템플릿 보간이 있는 곳은 **3곳뿐**이고:

- `2045`, `2054` — `escapeHtml(e.label)`, `escapeHtml(wf.name)` **처리됨**
- `1633` — 미이스케이프처럼 보이지만 보간값이 `NODE_TYPES[n.type].color` / `.svg`로
  **`index.html:1349`의 하드코딩 상수**다. 사용자 입력이 아니다.
  같은 줄의 사용자 값(`n.label`, `agent.name`, `agent.color`)은 전부 `escapeHtml()`을 거친다.

**결론: 이 경로들에서 사용자 입력이 이스케이프 없이 DOM에 들어가는 사례는 없다.**

> 다만 `NODE_TYPES[n.type]`는 `n.type`이 정의되지 않은 값이면 `undefined.color`로 **예외**가 난다.
> 보안이 아니라 견고성 문제이며, 손상된 워크플로우 JSON을 열 때 캔버스가 깨질 수 있다. (낮음)

---

## 고도화 전략

위 발견에서 직접 따라나오는 것만 적는다. 일반론은 넣지 않았다.

### 1단계 — 공개 사이트의 정체를 정한다 (A의 선행 조건)

지금 이 URL은 **"백엔드 있는 척하는 localStorage 데모"** 다. 둘 중 하나를 골라야 한다:

- **데모/포트폴리오로 확정** → localStorage 모드를 *의도된 동작*으로 명시.
  "서버 미연결 — 로컬 저장" 배너를 명확히 하고, 서버 전용 기능(팀 대시보드·MCP 패널)은 숨긴다.
  지금은 눌러도 아무 일이 없어 고장처럼 보인다.
- **팀 도구로 확정** → `WF_ACCESS_TOKEN` + 인증 방식(위 3안) → `API_BASE` 연결.

**결정 전까지 A는 착수하지 말 것.** 방향이 반대인 작업이다.

### 2단계 — 프론트에도 계약 테스트를 건다

백엔드에서 세 번 나온 "선언은 있고 구현은 없음"이 프론트에도 있다(C).
Phase 1-2의 MCP 계약 테스트를 확장해, **UI가 호출하는 API 경로가 실재하는지** 정적 검사한다.

**이 검사를 실제로 돌려봤다:**

```
UI 호출 고유 경로: 32   서버 라우트: 83
매칭 실패: 0건
```

`/api/messages`, `/api/sessions`, `/api/tests`가 처음 실패로 잡혔으나 **쿼리스트링을 못 거른
정규식 문제**였고, 셋 다 `server.js`에 GET·POST가 모두 존재한다. **죽은 엔드포인트는 없다.**

> 즉 UI↔서버 경로 정합성은 현재 건강하다. 다만 **지금 건강하다는 사실 자체를 아무도 지키고 있지 않다** —
> 라우트 이름 하나만 바뀌어도 조용히 깨지고, 그때는 사용자가 클릭해야 알게 된다.
> 위 스크립트는 20줄이면 되고 이미 동작을 확인했다. `package.json`의 `test`(현재 `exit 1`)에
> 이것부터 넣는 것이 가장 값싼 첫 걸음이다.

참고로 이 검사는 **경로 존재만** 본다. 메서드 일치(UI가 PUT을 부르는데 서버는 POST만 있는 경우)와
응답 형태까지 보려면 한 단계 더 필요하다.

### 3단계 — 단일 파일 5,472줄의 한계

현재는 빌드 없는 단일 HTML이라 배포가 단순하다는 실질적 장점이 있다. 성능도 gzip 67KB로 문제없다.
**지금 당장 쪼갤 이유는 없다.** 다만 다음 조건 중 하나가 오면 분할을 검토한다:

- 여러 사람이 동시에 편집해 충돌이 잦아질 때
- 테스트를 붙이고 싶은데 함수 단위 임포트가 불가능해 막힐 때

> 선제적 리팩터링보다, 2단계의 계약 테스트가 먼저다. 깨지는 걸 잡는 게 구조를 바꾸는 것보다 싸다.

### 4단계 — 그 다음

`fonts/` 정리(B) · 접근성(C) · 정적 manifest(D)는 서로 독립이라 언제든 병렬로 처리 가능하다.

---

## 검증 범위

**직접 확인함:** 배포본과 로컬 소스 동일성 · gzip 67KB · CORS 200 + 허용 헤더 ·
`requireAuth` 무토큰 통과 경로 · 폰트 3개 200 응답과 바이트 크기 · Isamanru 참조 0회 ·
manifest 3종 404 · `aria`/`role`/`button` 개수 · `innerHTML` 보간 3곳 전수 추적

**확인하지 않음:** 실제 브라우저 렌더링·상호작용(스크린샷/E2E 미실행) · Lighthouse 점수 ·
모바일 레이아웃 · 서버 전용 기능의 UI 동작 · 스펙의 "API 78개" 등 미검증 주장은 인용하지 않음
