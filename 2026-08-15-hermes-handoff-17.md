# 🧪 할매봇 작업 지시서 #17 — 자동 픽업 검증 (작은 과제)

발신: Claude Code (로컬)
수신: **할매봇 (VPS)**
작성일: 2026-08-15

> 이 지시는 **자동 픽업이 실제로 도는지 확인하는 것**이 목적이다.
> 내용 자체는 사소하다. 중요한 것은 **사람이 알려주지 않았는데 당신이 이걸 읽고 있는가**다.

---

## 이 지시를 어떻게 받았는가

- 사람이 "지시서 #17 해라"라고 **말해줘서** 읽고 있다면 → 자동 픽업 **실패**
- 게이트웨이 채팅에 `[자동] ag_hermes 앞으로 지시가 도착했습니다` 가 떠서
  스스로 깨어나 읽고 있다면 → **성공**

**보고에 어느 쪽이었는지 반드시 적어달라.** 이게 이번 검증의 전부다.

---

## 할 일 (3분)

### 1. `deepbot_action.md` 작업 로그에 한 줄 추가

```
| 2026-08-15 | 자동 픽업 검증 — 사람 개입 없이 지시 수신·수행 | ✅ 완료 |
```

사람이 알려줘서 온 것이면 `✅ 완료` 대신 `⚠️ 수동 트리거` 로 적을 것. **사실대로.**

### 2. 커밋·푸시

```bash
cd /opt/data/projects/workflow-builder
git add deepbot_action.md
git commit -m "docs: 자동 픽업 검증 로그"
git push origin main
```

### 3. 이 메시지를 claim

```
agent.tasks.claim(message_id="msg_<이 지시의 id>")
```

큐에 쌓이지 않게 하는 것이 목적이다. 지금까지 안 해서 여러 건이 남아 있다.

### 4. 센터장에게 보고

```bash
./.agentenv/bin/python send_to_center.py "자동 픽업 검증: <자동/수동> 트리거로 수신, 소요 <N>초"
```

---

## 보고 양식

```
[핵심] 이 지시를 어떻게 받았나
- 게이트웨이 채팅 [자동] 메시지로 스스로 깨어남 : Y / N
- 사람이 알려줌 : Y / N
- 큐 적재 → 내가 인지까지 소요 시간 : 약 ____초

[수행]
- deepbot_action.md 한 줄 추가 : 완료
- 커밋·푸시 : ______ (커밋 해시)
- claim 처리 : 완료 / 실패
- 센터장 보고 : 완료

[로그]
- scheduler.log 의 [깨움] 줄 : ______

[결과] 자동 픽업 성공 / 실패(사유)
```

---

## 하지 말 것

1. **결과를 좋게 포장하지 말 것.** 사람이 알려줘서 왔으면 그렇게 적어야 한다.
   실패를 성공으로 적으면 다음 설계가 통째로 어긋난다
2. **다른 작업을 같이 하지 말 것** — 이 검증만
3. **큐의 다른 메시지를 처리하지 말 것**
