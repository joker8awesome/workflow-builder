# 🎨 할매봇 작업 지시서 #8 — UI 수정 배포 (짧음)

발신: Claude Code (로컬)
수신: **할매봇 (VPS)**
작성일: 2026-08-15
대상 커밋: **`209106d`**
VPS 경로: `/opt/data/projects/workflow-builder`

> **재시작 불필요.** `index.html` 만 바뀌었고 `server.js:257` 의
> `express.static(__dirname)` 이 파일을 그대로 서빙한다. `git pull` 이면 끝난다.

---

## ⚠️ 먼저 — 지시서 #7 이 아직 안 끝났다

실측으로 확인했다:

```
POST /api/ai/decide (인증 없음) → 400     ← 401 이 아니다 = 플래그가 꺼져 있다
```

**이 문서를 먼저 해도 되지만, #7 이 본체다.**
#7 을 끝내야 유료 LLM 라우트(`/api/ai/generate`, `/api/ai/decide`)가 닫힌다.
이 문서만 하고 "완료"로 두지 말 것.

권장 순서: **#8 (5분) → #7 (2단계, 사용자 확인 필요)**

---

## 무엇을 고쳤나

GitHub Pages 배포본을 Chrome 으로 띄워 점검해 찾은 4건이다.
소스만 봐서는 안 보여서 실제 렌더링·상호작용으로 확인했다.

| # | 문제 | 조치 |
|---|---|---|
| 1 | `--panel-text-faint` 대비 **3.54:1** (WCAG AA 4.5 미달) — 필터 버튼 등 **56곳**의 글자가 거의 안 보였다 | `#6b7681` → `#7f8a97` (**4.68:1**) |
| 2 | `#sidebar` 가 `overflow-y:visible` — 노드 팔레트 마지막 항목(bottom 901px)이 화면보다 아래면 **닿을 수 없었다** | `overflow-y:auto` |
| 3 | 팀 패널 제목이 `(15개 세션)` 하드코딩 — 로드 실패해도 15개가 있는 것처럼 보였다 (실제 에이전트는 **17명**) | 숫자 제거, 로드 성공 시에만 실제 수 표시 |
| 4 | 팀 패널 오류가 **"로드 실패"** 한 줄 — 정적 배포에 백엔드가 없어 실패하는 게 정상인데 고장으로 보였다 | `panelErrorHtml()` 로 상황 구분 (서버 없음 / 인증 필요 / 응답 오류) |

`loadTeamStatus` 의 `if (!j.success) return;` 도 패널을 빈 채로 남기던 경로라 함께 고쳤다.

> **GitHub Pages 는 이미 반영됐다** (푸시 시 자동 배포, 새 색상 포함 확인).
> 이 작업은 **VPS 가 서빙하는 UI** 를 맞추는 것이다.

---

## STEP 1 — 배포

```bash
cd /opt/data/projects/workflow-builder
git status --short          # 비어 있어야 한다
git pull origin main        # 209106d

npm test                    # 8스위트 120건 (index.html 변경이지만 확인은 해둔다)
```

**pm2 restart 는 하지 않아도 된다.** 정적 파일이라 다음 요청부터 새 파일이 나간다.
(다른 이유로 재시작해도 무해하다)

---

## STEP 2 — 검증

### 2-1. 파일이 실제로 바뀌었는가

```bash
grep -c "panel-text-faint: #7f8a97" index.html    # 1
grep -c "overflow-y:auto" index.html              # 2 이상
grep -c "panelErrorHtml" index.html               # 5
grep -c "15개 세션" index.html                     # 0  ← 하드코딩 제거됨
```

### 2-2. 서버가 새 파일을 내보내는가

```bash
curl -s http://127.0.0.1:3737/ | grep -c "panel-text-faint: #7f8a97"
```
→ **1** 이면 정상. 0 이면 캐시 문제 — `pm2 restart workflow-builder` 후 재확인.

### 2-3. 🧑 사용자가 눈으로 확인 (선택)

웹 UI 를 열고:
- 좌측 필터 버튼(`전체` `진행` `완료` `블로커`) 글자가 **읽히는지**
- 사이드바를 **스크롤**할 수 있는지 (노드 팔레트 아래쪽 `투표` `종료` 까지)
- `팀` 버튼을 눌렀을 때 오류 문구가 **원인을 설명하는지**

---

## 롤백

```bash
git checkout <이전 커밋> -- index.html
```

재시작 불필요. 되돌려도 기능은 그대로이고 보기만 이전 상태가 된다.

---

## 하지 말 것

1. **`--panel-text-faint` 를 다시 어둡게 만들지 말 것** — 대비가 AA 미달로 돌아간다.
   값을 바꿔야 한다면 배경 대비 **4.5:1 이상**인지 먼저 계산할 것
2. **`#sidebar` 의 `overflow-y` 를 `visible` 로 되돌리지 말 것** —
   화면이 짧은 기기에서 팔레트 하단 항목에 닿을 수 없게 된다
3. **팀 패널 제목에 숫자를 다시 하드코딩하지 말 것** — 에이전트 수는 변한다

---

## 보고 양식

```
[1] 배포
- pull 전 HEAD : ______
- pull 후 HEAD : ______  (209106d 이상)
- npm test     : __스위트 / __건

[2] 검증
- panel-text-faint #7f8a97 : ____ (1 기대)
- panelErrorHtml           : ____ (5 기대)
- "15개 세션" 잔존         : ____ (0 기대)
- 서버 응답에 새 색상      : ____ (1 기대)
- 🧑 필터 버튼 가독성      : 개선됨 / 그대로
- 🧑 사이드바 스크롤       : 됨 / 안 됨

[3] 다음
- 지시서 #7 착수 예정 : Y / N

[결과] 완료 / 진행 / 차단(사유)
```

작업 후 `deepbot_action.md` 의 `## 작업 로그` 에 기록하고,
**이어서 지시서 #7 을 진행할 것.**
