# 커멘드센터 — 올바른 구축을 위한 로드맵

작성일: 2026-08-15
기준 커밋: `648c51e` (84개)
전제: 이 시스템은 **이미 동작 중**이다. 새로 만드는 게 아니라 **신뢰할 수 있게 만드는** 단계다.

---

## 진단 — 지금 무엇이 문제인가

16차까지 기능은 빠르게 쌓았지만, 이번 검토에서 **같은 유형의 결함이 반복**됨을 확인했다.

| 사례 | 증상 | 공통 원인 |
|------|------|-----------|
| `workflow.list` | 50개 전부 `node_count: 0` | JSONB 이중 파싱 → 빈 `catch`가 삼킴 |
| `agent.list` | 메타데이터 전부 빈 값, 필터 무시 | **스키마만 선언하고 구현을 안 함** |
| 서버 카드 | `http://` 광고 (토큰 평문화 위험) | 프록시 뒤 `req.protocol` 미검증 |
| `mcp-router.js` DB 풀 | `server.js`만 고치고 여기는 누락 | 같은 설정이 **두 곳에 중복** |

**핵심 진단: 기능이 "선언"되면 완료로 간주되고, 실제 동작을 확인하는 단계가 없다.**
`agent.list`가 결정적 사례다 — 툴 스키마에 `capability` / `online_only` 파라미터가 버젓이
선언돼 있는데 `args`를 꺼내지도 않는다. 스펙 문서·MCP 스키마·UI는 이 기능이 있다고 말하지만
코드는 없다. **문서와 코드가 조용히 갈라져 있다.**

> 그래서 이 로드맵의 1순위는 새 기능이 아니라 **"선언 = 동작"을 보증하는 장치**다.

---

## Phase 0 — 지금 즉시 (승인 대기 중)

| # | 작업 | 상태 |
|---|------|------|
| 0-1 | `WF_ACCESS_TOKEN` 확인 — 미설정 시 `/api/agents` POST/DELETE 무인증 노출 | ⏸ VPS 접근 필요 |
| 0-2 | `mcp-router.js` 변경분 커밋 + VPS 배포 | ⏸ 승인 대기 |
| 0-3 | 에이전트 메타데이터 주입 (`ops/fill-agent-metadata.js --apply`) | ⏸ 승인 대기 |
| 0-4 | 배포 후 검증 — `agent.list(capability="research")` → 1명 | 0-2·0-3 이후 |

> ### ⚠️ 0-4에서 반드시 확인할 것 — 새 `agent.list` SQL은 **아직 실행된 적이 없다**
>
> `node --check`는 **문법만** 본다. 쿼리는 로컬 DB가 없어 한 번도 돌려보지 못했다.
> 이 로드맵의 주제가 "선언만 하고 검증 안 함"이므로, 내가 쓴 코드에도 같은 잣대를 적용한다.
>
> | 미검증 항목 | 실패 시 증상 | 근거 |
> |---|---|---|
> | `GROUP BY a.machine` (jsonb 그룹핑) | 쿼리 자체가 에러 — `could not identify an equality operator` | `machine`이 `jsonb`가 아니라 `json` 타입이면 실패. API 응답 모양으로 추정했을 뿐 **스키마를 직접 확인하지 않았다** |
> | `s.status IN ('running','working','waiting')` | `online`이 **항상 false** (조용히) | `/api/team/status`에서 복사. `agent_sessions.status`의 실제 값을 확인하지 않았다 |
>
> **두 번째가 특히 위험하다** — 에러 없이 조용히 틀리는, 이번에 잡은 버그와 정확히 같은 유형이다.
>
> 배포 전에 Phase 2-1의 `pg_dump --schema-only`를 먼저 하면 둘 다 한 번에 해소된다. 또는 VPS에서:
> ```sql
> \d agents                                  -- machine 컬럼 타입 확인 (jsonb여야 함)
> SELECT DISTINCT status FROM agent_sessions; -- 실제 상태값 확인
> ```

```bash
# 0-1
npx pm2 env 0 | grep -E 'WF_ACCESS_TOKEN|WF_MCP_OPEN|WF_VAULT_KEY'
# 0-3 (dry-run 먼저)
node ops/fill-agent-metadata.js
node ops/fill-agent-metadata.js --apply
```

**순서 고정: 0-2(배포) → 0-3(데이터).** 반대로 하면 검증이 통과할 수 없다.

---

## Phase 1 — 재발 방지 (최우선, 1~2일)

새 기능보다 먼저. 이걸 안 하면 Phase 2 이후에서 같은 버그가 계속 나온다.

### 1-1. 빈 `catch` 전수 정리
`mcp-router.js` 9곳 · `server.js` 5곳. **모든 버그가 여기 숨어 있었다.**
삼키더라도 `console.warn`은 남길 것. 삼키면 안 되는 곳은 에러를 올릴 것.

### 1-1.5. 🔴 세션 상태 어휘 정립 — `online` / `active_sessions` 복구

**2026-08-15 확정된 버그.** 배포 후 검증 중 발견.

`agent_sessions.status`에 실제로 기록되는 값은 **`idle` 하나뿐이다** (라이브 19세션 전부 `idle`).
그런데 `online`·`active_sessions`는 `IN ('running','working','waiting')`으로 계산한다.
**이 세 값을 쓰는 코드가 저장소에 한 군데도 없다.**

| 위치 | 쓰는 값 |
|---|---|
| `agent_orchestrator.py:52` | `'idle'` 하드코딩 |
| `server.js:777` (INSERT) | `status \|\| 'idle'` |
| `server.js:816` (UPDATE) | `status \|\| 'idle'` |
| `index.html` | `idle` / `ok` / `fail` |

**영향:**
- `agent.list`의 `online` → 영구 `false`
- `/api/team/status`의 `active_sessions` → 영구 `0`
- **팀 대시보드의 실시간 ●○ 표시가 처음부터 동작한 적이 없다** (스펙 §4에 기능으로 명시돼 있음)

**조치 (택1 후 양쪽 쿼리 동시 수정):**
1. 오케스트레이터가 노드 실행 시작 시 `'running'`으로 UPDATE하고 종료 시 되돌린다 (정공법)
2. 또는 `agent_credentials.last_used_at` 기반 liveness로 재정의 — MCP 인증 때마다 갱신되므로
   (`mcp-router.js:35`) 실제 활동 신호로 쓸 수 있다

> **이 건이 로드맵의 진단을 다시 증명한다.** `online`은 MCP 스키마에도, 스펙 §4에도,
> UI에도 "있는 기능"으로 적혀 있지만 데이터가 뒷받침한 적이 없다.
> 그리고 이번엔 **내가 `/api/team/status`에서 그 필터를 그대로 복사해 같은 버그를 한 번 더 만들었다.**
> 1-2의 계약 테스트가 있었다면 즉시 걸렸을 사례다.

### 1-2. MCP 툴 계약 테스트 (가장 중요)
툴 스키마에 선언된 **모든 파라미터가 실제로 동작하는지** 검증하는 테스트.
`agent.list` 같은 "선언만 된 기능"을 **자동으로 잡아낸다.**

```js
// 예: 선언된 파라미터가 결과를 바꾸는지 확인
const all = await call('agent.list', {});
const filtered = await call('agent.list', { capability: 'research' });
assert(filtered.agents.length < all.agents.length, 'capability 필터 미동작');
```

12개 툴 × 선언된 파라미터 전부에 대해 작성. `package.json`의 `test` 스크립트가
현재 `exit 1`이므로 여기서 함께 채운다.

### 1-3. JSONB 접근 헬퍼 일원화
`workflow.list`·`agent.list` 모두 같은 타입 분기를 손으로 반복 중이다.
헬퍼 하나로 통일해 세 번째 재발을 막는다.

### 1-4. 설정 중복 제거
DB 접속이 `server.js`·`mcp-router.js` 두 곳에 있다 (이번에 한쪽만 고쳐진 원인).
`mcp_server.py`·`agent_orchestrator.py`도 확인할 것.

---

## Phase 2 — 로컬 개발 환경 (2~3일)

**현재 코드를 로컬에서 실행할 수 없다.** 모든 검증이 프로덕션에서만 가능한 상태 = 가장 큰 구조적 위험.

| # | 작업 |
|---|------|
| 2-1 | VPS에서 `pg_dump --schema-only` → `schema.sql` 확보 (스펙 §8 "26 테이블" 실측 겸) |
| 2-2 | 로컬 PostgreSQL 17 + 스키마 적용 + 최소 시드 |
| 2-3 | `npm run dev` 한 줄로 뜨는지 확인 (`.env.example` → `.env`) |
| 2-4 | `dotenv` 도입 검토 — 현재 `.env`를 자동으로 읽지 않는다 |
| 2-5 | 로컬 실행 절차를 `MIGRATION.md`에 반영 (커밋 `648c51e`에서 갱신됨 — 검증 필요) |

> Phase 1의 테스트도 로컬 DB가 있어야 제대로 돌릴 수 있으므로, 1과 2는 병행이 현실적이다.

---

## Phase 3 — 문서/데이터 정합성 (1~2일)

| # | 작업 |
|---|------|
| 3-1 | 스펙 문서 갱신 — §12에 실제 환경변수 7종, §8 테이블 수 실측 반영 |
| 3-2 | 템플릿 중복 정리 — `wf_tpl_team` vs `wf_tpl_team_mstiqejr` 등 **정본 결정 후** 삭제 |
| 3-3 | 8/14 대량 쓰기 종결 — `updated_at` 트리거 유무 확인 (spec-review §2의 SQL) |
| 3-4 | 단일 주소 의존 해소 — 생 IP가 스펙·`.mcp.json`·서버카드 3곳에 하드코딩. IP 변경 시 동시 파손 |

---

## Phase 4 — 그 다음에야 신규 기능

Phase 1~3이 끝나야 여기 진입한다. 후보:

- **trust_score 실동작** — 지금은 전원 50 고정. 실행 성공/실패로 갱신하는 로직 필요
  (`agent.report`의 `task_status`가 이미 신호를 갖고 있다)
- **워커 연동** — `figma` · `telegram` · `ci` · `scan` · `vault` · `cron` · `doc` · `team`
  (현재 `tools`에 선언만 됨 → **Phase 1-2 테스트로 "선언만 된 상태"를 계속 감시**)
- 역할 라우팅 고도화 — `capability` 필터가 살아나면 `/api/team/next`를 능력 기반으로 확장 가능

---

## 우선순위 요약

```
Phase 0  승인 대기 중인 4건            ← 지금
Phase 1  재발 방지 (빈 catch·계약 테스트) ← 신규 기능보다 먼저
Phase 2  로컬 개발 환경                 ← Phase 1과 병행
Phase 3  문서/데이터 정합성
Phase 4  신규 기능 (trust_score·워커 연동)
```

**한 문장 원칙:**
> 지금 필요한 건 기능을 더 얹는 게 아니라, **이미 있다고 적힌 기능이 실제로 동작하는지 확인하는 장치**다.
> `agent.list`가 그 증거이고, 계약 테스트(1-2)가 그 해답이다.
