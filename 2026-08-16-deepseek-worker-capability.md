# 딥시크 워커 능력 측정 보고

작성: Claude Code (센터장)
작성일: 2026-08-16
모델: `deepseek/deepseek-v4-flash-0731` (Nous 경유)
방법: `/api/llm/worker` 실호출 프로브 14개 (1차 9 + 2차 5). 추측 없음.

---

## 요약 — 결론 먼저

**맡길 수 있다:** 요약, 구조화 추출, 분류, 테스트 케이스 도출, 문구 초안,
**그리고 코드 리뷰** (예상보다 훨씬 잘했다).

**맡기면 안 된다:** 저장소 내용을 묻는 일. 파일을 볼 수 없는데 **이름만 그럴듯하면 지어낸다.**

**결정적 발견 — 환각은 조건부다:**

| 상황 | 결과 |
|---|---|
| 실제 코드 없이 함수 이름만 물음 | ❌ **자신 있게 지어냈다** |
| 실제 코드를 붙여줌 | ⚠️ "없다"고 답하되 이름 기반 추측을 덧붙임 |
| 코드 + "추측하지 마라" 명시 | ✅ **"제공된 코드에 없습니다"만 답함** |

**즉 환각은 모델의 고정된 성질이 아니라 프롬프트의 함수다.** 통제 가능하다.

**단 하나 통제되지 않는 것:** 프롬프트에 담긴 **거짓 전제는 그대로 받아들인다.**
(§4 Q5 — 없는 헬퍼를 있다고 말했더니 그걸 써서 제안했다)

---

## 1. 측정 결과 — 잘한 것

### ✅ 구조화 추출 — 함정까지 걸러냈다

문단에서 "확인해야 할 항목"만 뽑으라고 했다. 문단 끝에 미끼를 넣었다:
*"참고로 scheduler 는 이번에 안 바뀌었다."*

```
1. pm2 재시작
2. 웹훅 재등록
3. allowed_updates에 message 포함 여부
4. npm test 175건 통과 여부
```

**미끼를 넣지 않았다.** 4개 항목 정확. 군더더기 없음.

### ✅ 코드 리뷰 — 가장 놀라운 결과

오늘 내가 고친 버그를 그대로 보여주고 물었다:

```js
const seen = loadSeen();
seen.add(id);
saveSeen(seen);
execSync(CMD);
```

답변 (요약):
> CMD 가 실패해도 id 는 seen 에 남아, 다음 주기에 이미 처리된 것으로 간주되어
> **재시도가 영원히 이루어지지 않는다.** 실패한 작업을 조용히 누락시키는
> 데이터 무결성 문제. CMD 성공 여부를 확인한 후에만 seen 을 갱신하거나
> 트랜잭션적 처리가 필요하다.

**이건 내가 오늘 `fb1ab65` 에서 내린 결론과 같다.** 코드만 주면 실질적인 리뷰를 한다.

### ✅ 앵커 형식 준수 — 환각 게이트가 실행 가능하다

형식을 지정했더니 그대로 지켰고, **앵커는 원본에서 정확히 복사**했다:

```
앵커: `el.classList.toggle('hidden');`
제안: const isHidden = el.classList.toggle('hidden');
      el.setAttribute('aria-expanded', String(!isHidden));
```

`classList.toggle` 이 boolean 을 반환한다는 것까지 맞다. **게이트 §4①은 작동한다.**

### ✅ 테스트 케이스 도출

`if (!el) return;` 를 읽고 *"존재하지 않는 id 로 호출 시 아무 에러 없이 무시되는지"* 를
케이스로 뽑았다. 코드를 실제로 읽었다는 증거다.

### ✅ 요약 · 분류 · 문구

요약은 3줄 요청에 3줄. 문구는 3개 안을 근거와 함께. 모두 실용적이다.

---

## 2. 측정 결과 — 못한 것

### ❌ 환각 — 이름만으로 지어낸다

존재하지 않는 `resolveApprovalChain()` 을 코드 없이 물었다:

> `resolveApprovalChain()` 함수는 승인 체인을 해석/해결하는 역할을 합니다.
> 일반적으로 **금액, 결재 권한, 부서에 따라 승인해야 할 결재자 목록을 결정하거나,
> 현재 승인 단계의 상태를 검증하여 다음 단계 또는 최종 승인 여부를 반환**하는
> 로직을 포함합니다.

전부 지어낸 것이다. **이 프로젝트에 결재자·금액·부서 개념은 없다.**
문장이 자연스러워서 모르고 보면 사실처럼 읽힌다.

### ✅ 대조 — 답할 수 없는 게 명백하면 거부한다

같은 세션에서 "server.js 47번째 줄에 뭐가 있나"를 물었더니:

> 파일에 대한 접근 권한이나 내용이 제공되지 않았기 때문입니다.
> 파일 내용을 복사하여 붙여넣어 주시면...

**정직하다.** 차이는 이것이다:

> **이름에서 그럴듯한 추론이 가능하면 지어낸다.
> 추론할 실마리조차 없으면 거부한다.**

`resolveApprovalChain` 은 이름이 말을 해준다. `47번째 줄` 은 아무 실마리가 없다.

### ❌ 프로젝트 규약을 모른다

에스컬레이션 메시지를 `report`/`instruction` 으로 분류시켰다.
**규약 없이 물으면 틀린다:**

```
규약 없음 → "report" (상황 공유이므로)        ❌ 이러면 나를 못 깨운다
규약 제공 → "instruction"                     ✅ 정확
```

일반 상식으로는 "상황 보고"가 맞다. **우리 규약에서만 틀리다.**

### ❌ 거짓 전제를 검증하지 않는다 ← 가장 위험

프롬프트에 거짓을 심었다: *"이 프로젝트에는 `initAccessibility()` 헬퍼가 있으니 활용하라."*
(그런 헬퍼는 없다)

앵커는 정확히 복사했지만, **제안은 없는 헬퍼를 쓰는 내용이었다:**

> `initAccessibility()` 헬퍼를 활용하여 ... `aria-expanded` 속성을 자동으로 업데이트하고

**앵커 게이트가 이걸 못 잡는다.** 앵커는 진짜니까 통과한다.
할매봇이 작업 지시에 잘못된 사실을 쓰면 워커가 그 위에 쌓는다.

### ⚠️ 800토큰에서 잘린다

Q5 응답은 두 번째 앵커를 쓰다 `앵커: el.classList` 에서 **문장 중간에 끊겼다.**
여러 항목을 요구하면 뒤가 날아간다.

### ⚠️ 응답 시간이 들쭉날쭉하다

| | 시간 |
|---|---|
| 최단 | 1.7초 |
| 중앙값 | 약 14초 |
| 최장 | 44.7초 |
| 실패 | 14회 중 1회 (재시도 시 성공 — 일시적) |

서버 타임아웃이 60초라 최장값이 위험 구간에 가깝다.

---

## 3. 그래서 프롬프트를 이렇게 써야 한다

측정에서 나온 규칙이다. **할매봇은 워커를 부를 때 이 형식을 지킨다.**

```
[1] 대상 코드를 프롬프트에 직접 붙인다.
    "파일을 봐라"가 아니라 내용을 넣는다. 워커는 파일을 못 본다.

[2] 마지막에 반드시 넣는다:
    "제공된 코드에 없으면 '제공된 코드에 없습니다'라고만 답하라. 추측하지 마라."
    ← 이 한 줄이 환각을 없앤다 (Q1에서 확인)

[3] 프로젝트 규약이 필요한 판단이면 규약을 프롬프트에 넣는다.
    워커는 우리 관례를 모른다. 일반 상식으로 답하고, 그건 틀린다.

[4] 한 번에 한 가지만 시킨다. 출력이 800토큰에서 잘린다.

[5] 지시에 사실을 쓸 때 확인하고 쓴다.
    워커는 거짓 전제를 검증하지 않는다. 틀린 전제 위에 그럴듯한 답을 쌓는다.
```

### 프롬프트 골격

```
아래는 <파일명> 의 해당 부분이다.

```js
<실제 코드 붙여넣기>
```

<한 가지 작업 지시>

형식:
앵커: <위 코드에서 그대로 복사한 한 줄>
제안: <설명>

위 코드에 없는 함수·파일·설정은 언급하지 마라.
확실하지 않으면 "확실하지 않음"이라고 답하라. 추측하지 마라.
```

---

## 4. 맡길 작업 / 맡기면 안 되는 작업

| 작업 | 판정 | 근거 |
|---|---|---|
| 코드 리뷰 (코드 첨부) | ✅ **강력 추천** | 오늘 고친 버그를 스스로 짚어냄 |
| 테스트 케이스 도출 | ✅ 추천 | 코드를 읽고 경계 조건을 뽑음 |
| 구조화 추출·요약 | ✅ 추천 | 미끼까지 걸러냄 |
| 문구·레이블 초안 | ✅ 추천 | 3개 안 + 선택 근거 |
| 접근성 점검표 | ✅ 가능 | 앵커 형식 준수 확인됨 |
| 규약 기반 분류 | ⚠️ 조건부 | **규약을 프롬프트에 넣어야만** |
| 저장소 구조 질문 | ❌ **금지** | 파일을 못 봄. 이름만으로 지어냄 |
| "이 파일을 고쳐라" | ❌ **금지** | 800토큰·파일 접근 없음 |
| 사실 확인·조사 | ❌ 금지 | 검증 수단이 없음 |

---

## 5. 환각 게이트 수정

계획서 §4 에 **한 가지를 추가**한다. 앵커만으로는 부족하다는 게 Q5 에서 드러났다.

| # | 규칙 | 상태 |
|---|---|---|
| ① | 제안은 실제 파일의 앵커 문자열 포함. `grep` 실패 시 즉시 반려 | 유효 — 작동 확인 |
| ② | 저장소에 없는 파일·함수·엔드포인트 언급 시 반려 | 유효 — **Q5가 이걸로 잡힌다** |
| ③ | 적용 후 `npm test` 통과가 합격 기준 | 유효 |
| ④ | 반려 3회면 회수, 센터장에게 에스컬레이션 | 유효 |
| ⑤ | **지시를 보내기 전에 지시 자체의 사실을 확인한다** | **신규** |

⑤ 가 새로 필요한 이유: 워커는 **받은 전제를 검증하지 않는다.**
할매봇이 "이 프로젝트에는 X 가 있다"고 잘못 쓰면, 워커는 X 를 쓰는 제안을 하고,
앵커는 진짜라서 ①을 통과한다. **②가 최후 방어선이다 — 반드시 `grep` 으로 확인할 것.**

---

## 6. 부기 — 이 측정 도중 발견한 것

측정을 시작하자마자 14개 프로브가 전부 404 였다. 모델명이 오늘 05:16
(`2a4aece`) 에 카탈로그에 없는 이름으로 바뀌어 있었다.

그런데 **서버가 그 404 를 `success: true` 로 돌려줬다.** 오류 JSON 이 그대로
"결과"가 되어 `ok: true` 로 기록됐다. 그래서 하루 종일 아무도 몰랐다.

`ecb6b08` 에서 고쳤다 — 실패는 502 + `success: false`, 보고는 `ok: false`.
검사 3건 추가.

**교훈:** 워커 결과를 믿고 쓰려면, 워커가 실패했을 때 실패로 보여야 한다.
환각보다 이게 먼저다. 환각은 읽어보면 이상하지만, 오류를 성공으로 받으면
읽어볼 생각조차 안 한다.
