# 할매봇 작업 지시서 #24 — 워커 배치 준비 (Phase 0)

발신: Claude Code (센터장)
수신: **할매봇 (VPS)**
작성일: 2026-08-16
대상 커밋: **`e005626`**
전체 계획: `2026-08-16-worker-fleet-plan.md` — **먼저 읽어라**

> 사용자가 워커를 배치해 커멘드센터 웹을 고도화하기로 했다.
> **너는 워커들에게 작업을 배분하고, 결과를 검수하고, 직접 적용하는 역할이다.**
> 이번 지시서는 그 준비 단계(Phase 0)다. **아직 워커를 부르지 마라.**

---

## 먼저 알아야 할 것 — 딥시크는 코딩 워커가 아니다

코드를 확인한 결과다. 추측이 아니다.

| 확인 | 근거 | 결과 |
|---|---|---|
| 코드 패치를 낼 수 있나 | `server.js` `/api/llm/worker` `max_tokens: 800` | ❌ 불가 |
| 파일을 읽고 쓰나 | prompt → LLM → 텍스트가 전부 | ❌ 없음 |

800토큰은 한국어 600자다. 리뷰 의견·테스트 케이스 목록·문구 초안에는 충분하지만
**5,589줄 파일의 diff 를 담을 수 없다.** 파일 접근도 없어 스스로 적용하지 못한다.

```
딥시크  →  제안만 낸다 (텍스트)
너      →  검수하고, 직접 적용하고, npm test 를 돌린다
나      →  계획을 세우고, 방향이 틀어지면 다시 잡는다
```

**"딥시크에게 이 파일을 고쳐줘"라고 시키지 마라.** 그건 구조적으로 불가능하고,
시키면 그럴듯한 거짓 답이 온다.

---

## STEP 1 — 배포 (내가 고친 것)

```bash
cd /opt/data/projects/workflow-builder
git pull origin main        # e005626
npm test                    # 11스위트 175건 기대
npx pm2 restart workflow-builder
```

### 무엇을 고쳤나 — 이거 없으면 워커 결과가 증발한다

`/api/llm/worker` 가 결과를 이렇게 기록하고 있었다:

```js
VALUES ('report', $1, 'ag_orch', $2, 'sent', $3)
```

- `to_agent='ag_orch'` — **네가 시켜도 결과는 `ag_orch` 로 간다.** 너에게 안 온다
- `status='sent'` — `list_pending` 은 `'pending'` 만 본다. **큐에서 아무도 못 본다**

오늘 `POST /api/messages` 에서 고친 것과 같은 사고다. 이제 `report_to` 로 수신자를
지정할 수 있고, 기본값은 호출한 에이전트다.

**워커를 부를 때 이렇게 해라:**

```bash
curl -s -X POST "$BASE/api/llm/worker" \
  -H "Authorization: Bearer $KEY" -H 'Content-Type: application/json' \
  --data-binary @request.json
```

`request.json` (UTF-8 파일로 만들 것 — 명령줄에 한글을 직접 넣으면 깨진다):

```json
{
  "prompt": "...",
  "agent_id": "ag_deepseek",
  "report_to": "ag_hermes",
  "trace_id": "fleet-a11y-20260816"
}
```

---

## STEP 2 — 🔴 `index.html` 분해 (이번 작업의 본체)

**이게 관문이다. 이게 끝나기 전에는 병렬 작업이 불가능하다.**

### 왜

`index.html` 은 한 파일에 **5,589줄 / 281KB**. HTML·CSS·JS 가 전부 들어 있다.
워커를 여럿 붙여도 이 파일은 병렬 편집이 안 된다 — 같은 파일에 대한 여러 제안은
충돌하고, 움직이는 281KB 표적에 대한 패치는 만들자마자 낡는다.

### 어떻게

**새 구조를 발명하지 마라. 이미 코드에 있는 경계를 그대로 파일로 옮겨라.**

```
index.html          ← 뼈대만 남긴다 (마크업 + <link> + <script>)
css/main.css        ← <style> 내용
js/                 ← <script> 내용을 기능 단위로
```

기준:
- **파일당 1,000줄 이하**
- 함수를 쪼개거나 이름을 바꾸지 마라 — **옮기기만 한다**
- 동작이 조금이라도 바뀌면 실패다. 이번 작업의 성공 기준은 "똑같이 동작한다"이다

경계는 이미 있다: 패널별 렌더 함수, `togglePanel`, `loadTeamStatus`,
`panelErrorHtml`, fetch 래퍼 등. 그 단위로 끊어라.

### 주의

- **서비스워커(`sw.js`) 캐시 목록을 확인해라.** 파일이 늘어나므로 캐시 대상이 바뀐다.
  안 고치면 배포 후 옛 파일이 남아 화면이 깨진다
- **GitHub Pages 배포본도 같은 구조여야 한다.** 경로가 어긋나면 정적 사이트가 죽는다
- 분해 중간에 커밋하지 마라. **동작하는 상태에서 한 번에 커밋해라**

---

## STEP 3 — 회귀 확인

```bash
npm test                    # 175건
```

그리고 **육안으로** 확인해라. 테스트가 프론트를 보지 못한다:

- 페이지가 뜨는가
- 사이드바 패널 토글이 되는가
- `에이전트` / `🤝 팀 현황` 버튼이 각각 동작하는가
- 콘솔에 에러가 없는가

하나라도 어긋나면 **되돌리고 보고해라.** 억지로 고치지 마라.

---

## STEP 4 — 보고

분해 결과 파일 목록을 반드시 포함해라. **다음 단계(워커 배분)를 내가 그걸 보고 짠다.**
파일 이름을 모르는 채로 배분하면 존재하지 않는 파일을 가리키게 된다.

---

## 🔴 프로토콜 — 나를 깨우는 법

**`report` 로 보낸 것은 나를 깨우지 못한다.** 내 폴링은 이렇게 동작한다:

```
report              → 파일에 적고 끝. 세션 안 뜬다
instruction/command → 세션을 띄운다
```

| 상황 | type |
|---|---|
| 단위 완료, 정상 진행 | `report` |
| **분해 중 판단이 안 서는 지점** | `instruction` |
| **`npm test` 가 깨짐** | `instruction` |
| **계획이 현실과 다름** | `instruction` |
| 애매하면 | `instruction` |

**잘못 깨우는 비용보다 놓치는 비용이 크다.** 애매하면 `instruction` 으로 보내라.

`trace_id` 는 `fleet-phase0-20260816` 으로 유지해라.

---

## 하지 말 것

1. **아직 워커를 부르지 마라** — Phase 0 이 끝나야 배분할 수 있다
2. **딥시크에게 코드를 쓰게 하지 마라** — 800토큰, 파일 접근 없음. 거짓 답이 온다
3. **함수를 쪼개거나 이름을 바꾸지 마라** — 이번엔 옮기기만 한다
4. **분해 중간 상태로 커밋하지 마라** — 동작하는 상태에서 한 번에
5. **`report` 로 에스컬레이션하지 마라** — 조용히 묻힌다

---

## 보고 양식

```
[1] 배포
- HEAD : ______  (e005626 기대)
- npm test : __스위트 / __건  (11/175 기대)
- pm2 restart : 완료

[2] 분해
- 생성된 파일 (경로 + 줄 수) :
    ______
- index.html 최종 줄 수 : ______  (5,589 → ?)
- 1,000줄 넘는 파일 : 없음 / ______
- sw.js 캐시 목록 갱신 : 완료 / 불필요(사유)

[3] 회귀
- npm test : ______
- 페이지 로드 : Y / N
- 패널 토글 : Y / N
- 에이전트 / 팀 현황 버튼 : Y / N
- 콘솔 에러 : 없음 / ______

[결과] 완료 / 진행 / 차단(사유)
```

작업 후 `deepbot_action.md` 의 `## 작업 로그` 에 기록할 것.

---

## 다음 단계 미리보기 (지금 하지 마라)

분해가 끝나면 내가 워커 배분 지시서를 만든다. 예상 단위:

| 단위 | 딥시크가 낼 것 | 네가 할 것 |
|---|---|---|
| 접근성 | 요소별 점검표·수정 제안 | 적용·검증 |
| 서비스워커 | 안 쓰는 폰트 1.9MB 캐시 검토 | 적용·용량 측정 |
| 오류 표시 통일 | 누락 지점 목록 | 적용 |
| 문구 정리 | 문구 초안 | 채택·적용 |
| 프론트 테스트 | 테스트 케이스 목록 | 테스트 작성 |

그리고 **환각 게이트**가 붙는다 (계획서 §4). 요약:

- 모든 제안은 **실제 파일에서 복사한 앵커 문자열**을 포함해야 한다. `grep` 해서
  없으면 **즉시 반려. 디버깅하지 마라** — 파일을 안 보고 지어냈다는 뜻이다
- 저장소에 없는 파일·함수·엔드포인트를 언급하면 반려
- 적용 후 **`npm test` 통과가 합격 기준**이다. 워커의 자기 평가는 근거가 아니다
- **반려 3회면 회수하고 나에게 올려라** — 무한 재검토는 큐를 태운다
