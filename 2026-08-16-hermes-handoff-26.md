# 할매봇 작업 지시서 #26 — Phase 1: 워커 3개 병렬 배분

발신: Claude Code (센터장)
수신: **할매봇 (VPS)**
작성일: 2026-08-16
기준 커밋: `2175ef0` (분해 완료본)
참조: `2026-08-16-deepseek-worker-capability.md` — **워커 부르기 전에 읽어라**

> 분해 잘했다. `index.html` 5,589 → 1,011줄, css 2 + js 16, 회귀 6/6.
> 이제 병렬이 가능하다. **단위 3개를 동시에 진행한다.**

---

## 배분 원칙 — 파일이 겹치지 않는다

세 단위가 **서로 다른 파일**을 고친다. 그래서 동시에 가도 충돌하지 않는다.

| 단위 | 워커가 낼 것 | 네가 고칠 파일 |
|---|---|---|
| **A. 접근성** | 버튼 유형별 규칙 + 점검표 | `index.html` |
| **B. 코드 리뷰** | 함수별 결함 지적 | `js/coreb.js` |
| **C. 오류 문구** | 문구 개선안 | `js/ux6.js`, `js/corea.js` |

**이 배정을 바꾸지 마라.** 파일이 겹치는 순간 병렬의 의미가 없어진다.

---

## 공통 — 워커 호출 방법

측정으로 확인된 형식이다. **셋 다 이 골격을 지켜라.**

```
아래는 <파일명> 의 해당 부분이다.

<실제 코드 붙여넣기>

<한 가지 작업 지시>

형식:
앵커: <위 코드에서 그대로 복사한 한 줄>
제안: <설명>

위 코드에 없는 함수·파일·설정은 언급하지 마라.
확실하지 않으면 "확실하지 않음"이라고 답하라. 추측하지 마라.
```

호출 (한글은 반드시 UTF-8 파일로 — 명령줄에 직접 넣으면 깨진다):

```bash
curl -s -X POST "http://127.0.0.1:3737/api/llm/worker" \
  -H "Authorization: Bearer $KEY" -H 'Content-Type: application/json' \
  --data-binary @/tmp/task.json
```

```json
{ "prompt": "...", "agent_id": "ag_deepseek",
  "report_to": "ag_hermes", "trace_id": "fleet-a-20260816" }
```

### 🔴 반드시 지킬 것

1. **파일을 보라고 하지 마라. 코드를 붙여 넣어라.** 워커는 파일을 못 본다
2. **마지막 두 줄("언급하지 마라 / 추측하지 마라")을 빼지 마라.** 이게 환각을 가른다
3. **한 번에 한 가지만.** 출력이 800토큰에서 문장 중간에 잘린다
4. **🔴 네가 쓴 지시의 사실을 먼저 확인해라.** 워커는 받은 전제를 검증하지 않는다.
   없는 헬퍼를 "있다"고 했더니 앵커는 정확히 복사해놓고 그걸 쓰는 제안을 했다 —
   **앵커 검사를 통과한다.** 최후 방어선은 `grep` 이다

---

## 단위 A — 접근성 (`index.html`)

### 현황 (내가 실측했다)

```
버튼      97개
aria-*     3개
```

`btn-agents` 하나에만 `aria-label` 이 있다. 나머지는 없다.

문제가 분명한 예:

```html
<button id="sidebar-toggle">☰</button>
```

**아이콘만 있는 버튼이다.** 스크린리더가 읽을 것이 없다.

### 워커에게 시킬 것

97개를 워커에게 넘기지 마라. 출력이 잘린다.
**대표 샘플 6개를 붙이고 "유형별 규칙"을 받아라.** 적용은 네가 한다.

프롬프트에 붙일 코드 (`index.html` 643~654행에서 그대로):

```html
<button id="sidebar-toggle">☰</button>
<button id="btn-run" class="tb-action">▶ 실행</button>
<button id="btn-ai" class="tb-action">✨ AI</button>
<button id="btn-agents" class="tb-action" aria-label="에이전트 목록">에이전트</button>
<button id="btn-share" class="tb-action">공유</button>
<button id="btn-export" class="tb-action">내보내기</button>
```

지시:
> 위 버튼들을 접근성 관점에서 유형별로 분류하고, 유형마다 필요한 조치를 한 줄로 제시하라.
> 이미 조치된 것은 "이미 됨"이라고 적어라.

받은 규칙을 **네가 97개에 적용한다.** 워커가 개별 버튼을 다루게 하지 마라.

### 검수

- 규칙에 나온 속성이 실제 표준인지 확인 (`aria-label`, `aria-expanded`, `aria-controls` 등)
- **저장소에 없는 헬퍼 함수를 쓰라고 하면 반려** — 실제로 이 실수를 하는 것을 확인했다

---

## 단위 B — 코드 리뷰 (`js/coreb.js`, 632줄)

### 왜 이 단위인가

측정에서 **가장 잘한 항목**이다. `queue-trigger.js` 의 seen 버그를 보여줬더니
"재시도가 영원히 이루어지지 않는다"까지 스스로 짚었다. 내가 내린 결론과 같았다.

### 워커에게 시킬 것

**함수 단위로 쪼개서 여러 번 호출해라.** 632줄을 한 번에 넣지 마라.

각 호출:
> 다음 자바스크립트의 문제를 한 문단으로 지적하라. 문제가 없으면 "문제 없음"이라고만 답하라.
>
> (함수 하나 붙여넣기)
>
> 맥락: (언제 호출되는지, 실패하면 어떻게 되는지)

**맥락 한 줄이 결과를 크게 바꾼다.** "1분마다 실행된다"를 넣었더니 재시도 문제를 짚었다.

### 검수

- 지적이 **실제 코드에 근거하는가** — 붙여넣지 않은 코드를 언급하면 반려
- 고칠지 말지는 **네가 판단해라.** 워커 지적이 전부 옳은 건 아니다
- 고쳤으면 `npm test` 통과 확인

---

## 단위 C — 오류 문구 (`js/ux6.js`, `js/corea.js`)

### 현황

```js
(what || '데이터') + '을(를) 불러오지 못했습니다.<br>
  <span style="font-size:11px">서버 응답 오류 — 잠시 후 다시 시도하세요</span>'
```

```js
toast('테스트 케이스가 없습니다')
toast('시작 노드가 없습니다')
```

### 워커에게 시킬 것

측정에서 문구는 잘했다 (3개 안 + 선택 근거). **현재 문구 목록을 붙이고 개선안을 받아라.**

지시:
> 위 문구들을 사용자에게 더 도움이 되게 다듬어라. 각 문구마다 1개 안만 제시하라.
> 무엇이 잘못됐는지와 다음에 무엇을 하면 되는지가 드러나야 한다.

### 🔴 검수 — 여기서 워커가 사고를 친다

측정에서 워커가 **존재하지 않는 지원 이메일(`help@example.com`)을 지어냈다.**
빈칸을 그럴듯한 것으로 채우는 성향이 있다.

- **연락처·URL·버튼 이름이 나오면 전부 확인해라.** 없는 것이면 지워라
- "고객 지원팀에 문의" 같은 문구도 **그런 창구가 없으면 반려**
- 이건 팀 내부 도구다. 외부 고객 응대 문구가 나오면 맞지 않는다

---

## 진행 순서

세 단위를 **동시에 시작**해라. 순서 의존이 없다.

```
A 접근성    → 워커 1회 → 규칙 받기 → 네가 index.html 적용
B 코드리뷰  → 워커 N회 → 지적 받기 → 네가 판단·적용
C 오류문구  → 워커 1회 → 문구 받기 → 네가 적용
                              ↓
                     각 단위 끝날 때마다 개별 보고
```

**단위별로 따로 보고해라.** 셋을 묶어서 한 번에 보고하면 어디서 틀어졌는지 못 찾는다.

`trace_id`: `fleet-a-20260816` / `fleet-b-20260816` / `fleet-c-20260816`

---

## 별건 — `fonts/` 1.9MB 미사용

확인했다. 저장소 어디에서도 참조하지 않는다:

```
fonts/Isamanru-Bold.woff     667KB
fonts/Isamanru-Light.woff    633KB
fonts/Isamanru-Medium.woff   623KB
```

`css`, `js`, `index.html`, `sw.js` 전부 검색했고 참조가 없다. 서비스워커 캐시에서도 빠졌다.

**확인만 하고 지우지는 마라.** 네 쪽에서 한 번 더 검색해서 결과를 보고해라.
지울지는 사용자가 정한다.

```bash
grep -rn "Isamanru\|fonts/" --include='*.html' --include='*.css' --include='*.js' . | grep -v node_modules
```

---

## 하지 말 것

1. **단위 간 파일 배정을 바꾸지 마라** — 겹치면 병렬이 무의미해진다
2. **워커에게 파일 전체를 넘기지 마라** — 800토큰에서 잘린다
3. **"추측하지 마라" 줄을 빼지 마라** — 환각이 돌아온다
4. **워커 제안을 그대로 믿지 마라** — 반려 3회면 회수하고 나에게 올려라
5. **`fonts/` 를 지우지 마라** — 보고만

---

## 보고 양식 (단위마다 따로)

```
[단위 ?] A접근성 / B코드리뷰 / C오류문구
- 워커 호출 횟수 : __
- 반려 : __회 (사유: ______)
- 적용한 파일 : ______
- npm test : __건
- 워커가 지어낸 것 : 없음 / ______   ← 있으면 반드시 적어라

[결과] 완료 / 진행 / 차단(사유)
```

**"워커가 지어낸 것" 항목을 비워두지 마라.** 없으면 "없음"이라고 적어라.
이걸 모아야 다음에 프롬프트를 고칠 수 있다.

---

## 프로토콜

- 단위 완료 → `report`
- **반려 3회 / 판단이 안 서는 지점 / `npm test` 깨짐 → `instruction`**

`report` 는 나를 깨우지 못한다. 애매하면 `instruction`.
