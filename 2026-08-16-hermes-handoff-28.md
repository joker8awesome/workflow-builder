# 할매봇 작업 지시서 #28 — Phase 2-1: 파일 이름 정리

발신: Claude Code (센터장)
수신: **할매봇 (VPS)**
작성일: 2026-08-16
기준 커밋: `696635a`
전체 계획: `2026-08-16-phase2-plan.md`

> `#27` 잘했다. 20 → 0, aria-label 23개 전부 고유, id 없던 버튼에는
> `cred-modal-close` 를 새로 붙였다. 내가 지시하지 않은 부분까지 맞췄다.
>
> 이번엔 **이름 정리**다. 다음 단계(프론트 계약 테스트)가 이걸 전제로 한다.

---

## 왜 지금인가

분해된 파일 이름이 **무의미한 게 아니라 틀렸다.**

| 파일 | 실제 내용 (첫 줄) | 이름이 말하는 것 |
|---|---|---|
| `agent1.js` | 가상화 렌더링 — 뷰포트 밖 노드 | 에이전트? |
| `market.js` | `loadSessions()` | 마켓? |
| `tests.js` | 테스트 + **더보기 드롭다운** + 엣지 로그 | 테스트만? |
| `ux1`~`ux6` | 실행 엔진 / Undo·Redo / 터치 / LLM 노드 | 아무것도 |

**이미 대가를 치렀다.** 지시서 #26 에서 내가 단위 C 를 `ux6.js` 에 배정했는데
실제 문구는 `tests.js`·`ux1.js` 에 있었다. 네가 알아서 찾아 고쳤지만,
그건 배정이 틀렸다는 뜻이다. 내가 `coreb.js` 에 코드 리뷰를 배정할 때도
안에 뭐가 있는지 모르고 배정했다.

다음 단계에서 테스트를 쓴다. **이름이 곧 바뀔 파일을 참조하는 테스트를 쓸 수는 없다.**

---

## STEP 1 — 각 파일이 무슨 일을 하는지 파악

### 워커를 쓰는 방법

파일마다 **함수 이름 목록과 첫 20줄**을 붙이고 이렇게 물어라:

> 아래는 한 파일의 함수 목록과 시작 부분이다.
>
> (붙여넣기)
>
> 이 파일이 무슨 일을 하는 파일인지 2~3단어로 답하라. 한 줄만 답하라.
>
> 위 내용에 없는 것은 언급하지 마라. 확실하지 않으면 "확실하지 않음"이라고 답하라. 추측하지 마라.

### 🔴 파일명을 워커에게 짓게 하지 마라

**설명만 받아라. 이름은 네가 정한다.**

워커는 빈칸을 그럴듯한 것으로 채우는 성향이 있다 (측정에서 없는 이메일을 지어냈다).
이름은 빈칸이 아니라 판단이다. 워커 설명을 근거로 삼되, **결정은 네가 해라.**

파일 16개를 한 번에 넣지 마라. **한 번에 한 파일**이다. 800토큰에서 잘린다.

---

## STEP 2 — 이름 바꾸기

### 규칙

- 내용을 나타내는 이름. `ux1`, `corea` 같은 순번·알파벳 금지
- 영문 소문자 + 하이픈. 예: `canvas-render.js`, `undo-redo.js`, `exec-engine.js`
- **파일을 쪼개거나 합치지 마라.** 이번엔 이름만 바꾼다
- 함수 이름은 건드리지 마라 — 전부 전역이라 파일 간 참조 수정이 필요 없다

`git mv` 를 써라. 이력이 이어진다.

### 🔴 같은 커밋에서 반드시 함께 바꿀 것

**① `index.html` 의 `<script src>` 16줄** (994~1009행)

**② `sw.js` 의 `ASSETS` 목록**

```js
e.waitUntil(caches.open(CACHE).then(c => c.addAll(ASSETS)));
```

`addAll` 은 목록 중 **하나라도 404 면 전체가 실패한다.** 부분 실패가 아니라
**서비스워커 설치가 통째로 죽는다.** 파일명만 바꾸고 ASSETS 를 안 고치면 그렇게 된다.

**③ `sw.js` 의 `CACHE` 를 `wf-builder-v6` → `wf-builder-v7`**

안 올리면 기존 사용자의 서비스워커가 **옛 목록을 계속 서빙한다.**
새 파일을 못 찾아 화면이 깨진다.

세 가지가 한 커밋에 없으면 배포 순간 사이트가 죽는다. **나눠 커밋하지 마라.**

---

## STEP 3 — 검증

```bash
npm test        # 178건
```

그리고 **숫자로 확인해라:**

```bash
# 남은 무의미 이름 — 0 이어야 한다
ls js/ | grep -cE '^(ux[0-9]|core[ab]|agent[0-9])\.js$'

# script src 와 실제 파일이 일치하는가 — 0 이어야 한다
node -e '
const fs=require("fs");
const h=fs.readFileSync("index.html","utf8");
const refs=[...h.matchAll(/<script src="(js\/[^"]+)"/g)].map(m=>m[1]);
const missing=refs.filter(p=>!fs.existsSync(p));
console.log("index.html 이 가리키는데 없는 파일:", missing.length, missing.join(" "));
'

# sw.js ASSETS 와 실제 파일이 일치하는가 — 양쪽 0 이어야 한다
node -e '
const fs=require("fs");
const sw=fs.readFileSync("sw.js","utf8");
const listed=[...sw.matchAll(/\x27\.\/(js\/[^\x27]+|css\/[^\x27]+)\x27/g)].map(m=>m[1]);
const actual=[...fs.readdirSync("js").map(f=>"js/"+f), ...fs.readdirSync("css").map(f=>"css/"+f)];
console.log("ASSETS 에 있는데 파일 없음:", listed.filter(p=>!actual.includes(p)).join(" ")||"없음");
console.log("파일 있는데 ASSETS 누락:", actual.filter(p=>!listed.includes(p)).join(" ")||"없음");
'

# CACHE 버전
grep -o "wf-builder-v[0-9]*" sw.js
```

### 화면 회귀 (육안)

Playwright 는 이미 있으면 쓰고, 없으면 설치하지 말고 눈으로 해라.

- 페이지가 뜨는가 · 콘솔 에러 없는가
- 사이드바 패널 토글
- `에이전트` / `🤝 팀 현황` 버튼
- **더보기(•••) 메뉴** — `tests.js` 안의 `MORE_ITEMS` 가 여기 관여한다. 이름이 바뀌는 파일이다
- 실행(▶) 한 번

---

## 하지 말 것

1. **파일을 쪼개거나 합치지 마라** — 이번엔 이름만
2. **함수 이름을 바꾸지 마라** — 전역이라 다른 파일이 부른다
3. **`sw.js` 를 다음 커밋으로 미루지 마라** — 그 사이 배포되면 사이트가 죽는다
4. **`CACHE` 버전 올리는 것을 빠뜨리지 마라** — 기존 사용자만 깨진다. 너는 못 본다
5. **워커에게 파일명을 짓게 하지 마라** — 설명만 받아라

---

## 보고 양식

```
[1] 파악
- 워커 호출 : __회 (파일 16개)
- 워커가 지어낸 것 : 없음 / ______

[2] 이름
- 변경 목록 (옛 → 새) :
    ______
- git mv 사용 : Y / N

[3] 검증 (숫자로)
- 남은 무의미 이름 : __      (0 기대)
- index.html 이 가리키는데 없는 파일 : __   (0 기대)
- ASSETS 에 있는데 파일 없음 : ______       (없음 기대)
- 파일 있는데 ASSETS 누락 : ______          (없음 기대)
- CACHE : wf-builder-v__                    (v7 기대)
- npm test : __스위트 / __건                (11/178 기대)

[4] 화면 회귀
- 페이지 · 콘솔 · 토글 · 버튼 · 더보기 · 실행 : ______

[결과] 완료 / 진행 / 차단(사유)
```

---

## 다음 (지금 하지 마라)

이름이 정리되면 **프론트 계약 테스트**를 만든다. 첫 번째가 이거다:

> `js/*.js` 의 모든 `getElementById('x')` 에 대응하는 `id="x"` 가 `index.html` 에 있는가

지금 `js/ux2.js:274` 는 이렇다:

```js
document.getElementById('btn-group').addEventListener('click', toggleGroup);
```

**널 가드가 없다.** id 가 어긋나면 그 줄에서 예외가 나고 이후 스크립트가 통째로 멈춘다.
프론트 검사는 지금 **0건**이다. 거기부터 채운다.

---

`trace_id`: `phase2-rename-20260816`
정상 완료 → `report` / 판단 필요 → `instruction`
