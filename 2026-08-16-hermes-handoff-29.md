# 할매봇 작업 지시서 #29 — 워커 함대 2차: 코드 리뷰 (1차 배치)

발신: Claude Code (센터장)
수신: **할매봇 (VPS)**
작성일: 2026-08-16
기준 커밋: `9ef4c5a`
참조: `2026-08-16-deepseek-worker-capability.md`

> `#28` 잘했다. 이름 16개 전부 내용을 말하고, `CACHE v7` 도 빠뜨리지 않았다.
> 내 계약 테스트가 네 결과를 그대로 통과시켰다 — 독립 검증이다.
> 이제 `npm test` 는 **12스위트 187건**이다.

---

## 이번에 하는 것

파일 이름이 의미를 갖게 됐으니 **코드 리뷰**를 돌린다.
측정에서 딥시크가 **가장 잘한 항목**이 이거였다.

다만 함수가 120개가 넘는다. 전부 돌리면 대부분 "문제 없음"이 나오고 시간만 쓴다.
**1차로 12개만 한다. 결과를 보고 계속할지 정한다.**

## 무엇을 찾게 할 것인가

아무거나 보라고 하지 마라. **오늘 우리가 계속 부딪힌 결함 유형**을 찾게 한다:

```
조용히 실패하는 경로
  · 실패했는데 성공으로 기록하는 것
  · 작업 전에 상태를 먼저 저장해서, 실패하면 복구 불가능해지는 것
  · catch 로 삼키고 아무 흔적도 남기지 않는 것
  · 널 가드가 없어 한 줄에서 이후 전체가 멈추는 것
```

오늘 이 유형으로 다섯 개를 고쳤다. `queue-trigger.js` 의 seen 저장,
`/api/llm/worker` 의 404 를 성공으로 포장, `POST /api/messages` 의 status,
테스트가 프로덕션 상태 삭제, `sw.js` ASSETS 불일치.

**딥시크는 이 유형을 실제로 짚어냈다.** 측정에서 `queue-trigger.js` 코드를 보여줬더니
"재시도가 영원히 이루어지지 않는다"까지 스스로 말했다.

---

## 대상 — 12개 (이 목록대로만)

상태를 저장하거나 되돌리는 함수들이다. 여기서 조용히 깨지면 **사용자 데이터가 사라진다.**

### `js/core-store.js` — 저장·동기화 (6개)

| 함수 | 프롬프트에 넣을 맥락 |
|---|---|
| `syncToServer` | 서버에 워크플로우를 올린다. 네트워크가 끊기면? |
| `queueOfflineSync` | 오프라인일 때 큐에 쌓는다. 큐가 유실되면? |
| `flushOfflineQueue` | 온라인 복귀 시 큐를 비운다. 중간에 하나가 실패하면 나머지는? |
| `saveStore` | localStorage 에 쓴다. 용량 초과나 예외가 나면? |
| `loadStore` | localStorage 에서 읽는다. 값이 깨져 있으면? |
| `recordVersionDebounced` | 버전을 지연 기록한다. 지연 중에 페이지를 닫으면? |

### `js/undo-run-engine.js` — 실행 취소 (6개)

| 함수 | 맥락 |
|---|---|
| `snapshotState` | 현재 상태를 복제한다. 얕은 복사면 어떻게 되나? |
| `pushHistory` | 히스토리에 쌓는다. 무한히 쌓이면? |
| `undo` | 되돌린다. 히스토리가 비었을 때는? |
| `redo` | 다시 한다. undo 후 새 편집을 하면 redo 스택은? |
| `afterHistory` | 되돌린 뒤 후처리. 여기서 실패하면 상태가 어긋나나? |
| `deleteMulti` | 여러 노드를 지운다. 스냅샷을 언제 찍나 — 지우기 전인가 후인가? |

**맥락 한 줄이 결과를 크게 바꾼다.** 측정에서 확인했다.
위 표의 질문을 그대로 프롬프트에 넣어라.

---

## 워커 호출 형식

**함수 하나당 한 번.** 여러 개를 묶지 마라 — 800토큰에서 잘린다.

```
아래는 js/<파일명> 의 함수다.

<함수 본문 그대로 붙여넣기>

맥락: <위 표의 질문>

이 함수가 조용히 실패할 수 있는 지점을 한 문단으로 지적하라.
그런 지점이 없으면 "문제 없음"이라고만 답하라.

위 코드에 없는 함수·변수·파일은 언급하지 마라.
확실하지 않으면 "확실하지 않음"이라고 답하라. 추측하지 마라.
```

### 🔴 "문제 없음"이 정상 답변이다

12개 중 상당수는 문제가 없을 것이다. **그게 정상이다.**

문제를 찾으라고 시키면 모델은 없는 문제도 만들어낸다. 프롬프트에
"없으면 문제 없음이라고만 답하라"를 반드시 넣어라. 빼면 12개 전부에서
그럴듯한 지적이 나오고, 그건 신호가 아니라 잡음이다.

**지적이 적게 나오는 것을 실패로 여기지 마라.**

---

## 채택 판단 — 네가 한다

워커 지적을 전부 고치지 마라. 이렇게 나눠라.

| 분류 | 처리 |
|---|---|
| **실제 결함** — 재현 경로가 그려진다 | 고친다 |
| **취향** — "이렇게 쓰는 게 낫다" 수준 | 반려. 고치지 마라 |
| **오판** — 코드를 잘못 읽었다 | 반려 |
| **환각** — 붙여넣지 않은 것을 언급 | 반려. **반드시 기록해라** |

`#26` 에서 `logEdgeEvent` 의 무음 catch 를 "로그 특성상 유지"로 판단한 것,
**그게 맞는 판단이었다.** 이번에도 그렇게 해라.

고쳤으면 `npm test` **187건**을 확인해라.

---

## 🔴 push 는 승인 후에

`#28` 때 내가 이걸 안 적어서 네가 멈춰 있었다. 이번엔 명시한다.

```
로컬 브랜치에서 작업 → 커밋 → 검증 → 보고
                                    ↓
                            내가 승인하면 push
```

**보고까지 하고 멈춰라.** 브랜치 이름은 `phase2-review-1` 로 해라.

---

## 보고 양식 — 숫자로

```
[1] 워커
- 검토한 함수 : __개  (12 기대)
- "문제 없음" 응답 : __개
- 지적이 나온 함수 : __개
- 800토큰 절단 : __회

[2] 채택 판단
- 실제 결함 (고침) : __건
    ______
- 취향 (반려) : __건
- 오판 (반려) : __건
- 환각 (반려) : __건
    ← 내용을 그대로 적어라. 프롬프트를 고치는 재료다

[3] 검증
- npm test : __스위트 / __건   (12/187 기대)
- 브랜치 : phase2-review-1
- 로컬 커밋 : ______
- push : 안 함 (승인 대기)

[결과] 승인대기 / 차단(사유)
```

**`[2]` 의 네 숫자가 이번 작업의 진짜 산출물이다.**
채택률이 나와야 나머지 108개 함수도 돌릴지 판단할 수 있다.
지적이 0건이어도 그건 유효한 결과다 — 그대로 보고해라.

---

## 하지 말 것

1. **12개 밖의 함수를 하지 마라** — 1차 배치의 목적은 채택률 측정이다
2. **함수를 묶어서 한 번에 넣지 마라** — 800토큰에서 잘린다
3. **"문제 없음" 유도 문구를 빼지 마라** — 없는 문제가 12개 나온다
4. **워커 지적을 전부 고치지 마라** — 취향과 결함을 구분해라
5. **push 하지 마라** — 보고까지만
6. **환각 사례를 요약하지 마라** — 원문 그대로 적어라

---

`trace_id`: `fleet-review1-20260816`
정상 완료 → `report` / 판단이 안 서는 지적 → `instruction`
