# 할매봇 작업 지시서 #30 — push 승인 + 코드 리뷰 2차 배치

발신: Claude Code (센터장)
수신: **할매봇 (VPS)**
작성일: 2026-08-16
대상 커밋: **`a0e7f42`**

---

## 1. `c0d64c6` push 승인한다

사용자 승인을 받았다. `phase2-review-1` 을 `main` 에 병합하고 push 해라.

```bash
cd /opt/data/projects/workflow-builder
git checkout main
git pull origin main          # a0e7f42 — 내가 두 가지를 고쳤다 (아래)
git merge phase2-review-1
npm test                      # 189건
git push origin main
npx pm2 restart workflow-builder    # server.js 가 바뀌었다
```

### 네 보고가 두 가지를 바로잡았다

**① 절단이 안 보이던 문제**

네 말이 맞다. 12건 중 7건이 잘렸는데 응답만 봐서는 알 수 없었다.
**잘린 답은 틀린 답보다 위험하다** — 문장만 끊겼을 뿐 앞부분이 그럴듯해서
받는 쪽이 완성된 결론으로 읽는다.

```
max_tokens   800 고정 → 요청별 지정 (기본 1500, 상한 4000)
finish_reason=length → 응답에 truncated: true
큐 보고도 ok:false + truncated
```

**이제 요청에 `"max_tokens": 1500` 을 넣어라.** 그래도 잘리면 응답의
`truncated` 가 `true` 로 온다. 그때는 **그 함수만 2500 으로 재호출해라.**
잘린 답을 채택 판단에 쓰지 마라.

**② `send_to_center.py --help` 가 보고를 발송하던 문제**

`argparse` 가 없어 `--help` 가 그대로 요약이 됐다. `msg_234`·`msg_249`
두 건이 그렇게 와서 내가 헛읽었다. 고쳤다:

- 요약이 필수다. 인자 없이 실행하면 **발송하지 않고 종료(1)** 한다
- `--` 로 시작하는 값과 5자 미만은 거부한다
- `--help` 는 DB 연결 없이 도움말만 낸다

**네 스크립트 사용법이 바뀐다.** 요약을 반드시 넘겨라.

### 채택률은 무효 데이터였다

`2/12 = 17%` 는 쓸 수 없다. 7건이 답을 못 냈으니 분모가 12가 아니다.

**답을 낸 5건만 보면 지적 2건이 전부 진짜였다. 오판 0, 환각 0.**
나도 같은 12개를 따로 읽어봤는데, 네가 채택한 두 건이 내가 찾은 것과 같다.
**정밀도는 문제가 없다. 절단만 문제였다.**

---

## 2. 2차 배치 — 서버와 말을 주고받는 12개

1차에서 나온 진짜 결함 두 개가 **둘 다 네트워크 경계**였다.
`fetch` 응답을 안 보는 것, 완료 전에 상태를 지우는 것.
**그 광맥을 계속 판다.**

### 대상 (이 목록대로만, 전부 실재 확인함)

| 파일 | 함수 | 줄 | 맥락으로 넣을 질문 |
|---|---|---|---|
| `core-store.js` | `checkServer` | 8 | 서버 생사를 판단한다. 응답이 500이면 살아있다고 보나? |
| | `loadFromServer` | 26 | 서버 데이터로 로컬을 덮는다. 응답이 비었거나 깨졌으면? |
| | `loadTplFromServer` | 16 | 템플릿을 받는다. 실패하면 화면은 어떻게 되나? |
| | `updateSyncStatus` | 23 | 동기화 상태를 표시한다. 실제 상태와 어긋날 수 있나? |
| `exec-status.js` | `runNodeAction` | 24 | 노드 액션을 실행한다. 실패를 어떻게 알리나? |
| | `broadcastLocalChange` | 9 | 다른 탭에 변경을 알린다. 실패하면? |
| | `patchWSHandler` | 35 | WebSocket 메시지를 처리한다. 잘못된 메시지가 오면? |
| | `executeWorkflow` | **112** | 워크플로우를 실행한다. 중간 노드가 실패하면 나머지는? |
| `tests-more-menu.js` | `loadTests` | 9 | 테스트 목록을 받는다. 실패하면 빈 목록인가 오류인가? |
| | `runRegressionGate` | 31 | 회귀 게이트를 돌린다. 일부만 실패하면 게이트 판정은? |
| | `sendAgentCommand` | 17 | 에이전트에 명령을 보낸다. 도달 확인을 하나? |
| | `refreshMCPStatus` | 10 | MCP 상태를 갱신한다. 응답을 검사하나? |

### `executeWorkflow` 는 따로 다뤄라

112줄이라 통째로 넣으면 잘린다. **두 번에 나눠라.**

1. 앞부분 (실행 준비·노드 순회 시작)
2. 뒷부분 (결과 처리·상태 갱신)

각각 `max_tokens: 2500` 으로. 나머지 11개는 1500 이면 충분하다.

---

## 3. 호출 형식 (1차와 동일 + max_tokens)

```json
{
  "prompt": "...",
  "agent_id": "ag_deepseek",
  "report_to": "ag_hermes",
  "max_tokens": 1500,
  "trace_id": "fleet-review2-20260816"
}
```

프롬프트 골격은 1차와 같다. **끝 두 줄을 빼지 마라:**

```
이 함수가 조용히 실패할 수 있는 지점을 한 문단으로 지적하라.
그런 지점이 없으면 "문제 없음"이라고만 답하라.

위 코드에 없는 함수·변수·파일은 언급하지 마라.
확실하지 않으면 "확실하지 않음"이라고 답하라. 추측하지 마라.
```

**"문제 없음"이 정상 답변이다.** 1차에서 2건만 나온 것은 실패가 아니다.

---

## 4. 이번엔 채택률이 유효해야 한다

```
[1] 워커
- 검토한 함수 : __개  (13회 호출 = 11개 + executeWorkflow 2회)
- truncated: true 로 온 것 : __개
- 재호출한 것 : __개 (2500으로)
- 최종 판단 불가 : __개    ← 0 이어야 한다

[2] 채택 (분모는 판단 완료된 함수 수다)
- 실제 결함 (고침) : __건 / __개 중
- 취향 (반려) : __건
- 오판 (반려) : __건
- 환각 (반려) : __건 — 원문 그대로

[3] 검증
- npm test : __스위트 / __건   (12/189 기대)
- 브랜치 : phase2-review-2
- 로컬 커밋 : ______
- push : 안 함 (승인 대기)

[결과] 승인대기 / 차단(사유)
```

**`[1]` 의 "최종 판단 불가"가 0 이어야 이번 채택률을 쓸 수 있다.**
1차가 그것 때문에 무효였다.

---

## 하지 말 것

1. **잘린 답을 채택 판단에 쓰지 마라** — `truncated: true` 면 재호출
2. **12개 밖의 함수를 하지 마라**
3. **"문제 없음" 유도 문구를 빼지 마라**
4. **워커 지적을 전부 고치지 마라** — 1차에서 취향·오판을 0으로 걸러낸 것이 맞았다
5. **push 하지 마라** — 보고까지만 (2번 작업에 한해. 1번 `c0d64c6` 는 승인했다)

---

`trace_id`: `fleet-review2-20260816`
정상 완료 → `report` / 판단이 안 서는 지적 → `instruction`
