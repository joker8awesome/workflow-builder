# 할매봇 작업 지시서 #32 — 웹 UX 고도화 배치 A·B·C

발신: Claude Code (센터장)
수신: **할매봇 (VPS)**
작성일: 2026-08-16
기준 커밋: `aef6d35`
**상시 지침: `2026-08-16-web-ux-directive.md` — 먼저 읽어라. 이 지시서는 배치만 지정한다.**

> `#31` 잘했다. 모델을 확인해 `moonshotai/kimi-k3` 를 보고했고,
> 짐작해서 고치지 않았다. 그 값이 있어야 능력 재측정을 할 수 있었다.
>
> Kimi 재측정 결과는 지침서 §4 에 반영했다. **이름만으로 지어내는 위험은 줄었지만,
> 거짓 전제를 받아들이는 구멍은 그대로다.** 게이트를 느슨하게 하지 마라.

---

## 이번 배치

| 배치 | 내용 | 워커 | 합격 기준 |
|---|---|---|---|
| **A** | Esc 로 패널 닫기 | 필요 | Esc 안 되는 패널 **0개**, 새 리스너 **1개** |
| **B** | 포커스 이동·복귀 + `role="dialog"` | 필요 | 세 항목 전부 **0개** |
| **C** | `css/mobile.css` → `css/cred-modal.css` | 불필요 | `npm test` 통과 |

A·B 는 같은 패널 15개를 만진다. **순서대로 해라. 병렬로 하지 마라** — 같은 파일이다.
C 는 독립이므로 먼저 해도 되고 나중에 해도 된다.

---

## 배치 A — Esc 로 닫기

### 실측

```
패널·모달        15개
Escape 핸들러     2개  (둘 다 패널과 무관 — 인라인 편집 취소, 팔레트)
```

패널을 열고 Esc 를 눌러도 아무 일도 없다.

### 🔴 리스너를 15개 만들지 마라

**위임 하나로 처리해라.** 가장 위에 열린 패널을 닫는 `keydown` 리스너 하나다.
패널마다 달면 나중에 패널이 늘 때 반드시 빠뜨린다.

워커에게 물을 때 패널 마크업 샘플 2개와 기존 토글 함수를 붙이고
**"위임 방식으로"** 라고 명시해라. "패널마다 리스너를 달라"는 답이 오면 반려한다.

### 검증

```bash
# 새로 만든 리스너가 1개인지 — 15개면 실패다
grep -c "Escape" js/*.js
```

브라우저에서 패널 3개를 열어 Esc 로 닫히는지 눈으로 확인해라.

---

## 배치 B — 포커스 이동과 복귀

### 실측

```
포커스 관련 코드   5줄
role="dialog"      1개   (자격증명 모달만)
```

패널을 열어도 포커스가 뒤에 남는다. 키보드 사용자는 Tab 을 수십 번 눌러야 닿는다.

### 목표

- 열리면 패널 안으로 포커스 이동
- 닫으면 **열었던 버튼으로 복귀**
- `role="dialog"` + `aria-modal="true"`

### 🔴 여기서 워커가 틀리기 쉽다 (지침서 §4 ⑥)

ARIA 는 **어느 요소에 붙느냐가 핵심**인데 앵커 검사로는 못 잡는다.
앵커는 진짜인데 위치가 틀릴 수 있다.

실제로 겪었다 — 딥시크가 `aria-expanded` 를 패널에 붙이라고 했고 나는 그걸
정확하다고 적었다. 틀렸다. 그건 **여닫는 버튼**에 붙는다.
모델을 바꿔 다시 재보고서야 알았다.

**제안이 오면 속성이 붙는 요소가 맞는지 확인해라:**

```
aria-expanded   → 여닫는 버튼에 (패널 아님)
role="dialog"   → 대화상자 컨테이너에
aria-modal      → 같은 컨테이너에
```

### 검증

```bash
grep -c 'role="dialog"' index.html      # 15 이상
grep -c "aria-modal" index.html         # 15 이상
```

키보드만으로 패널을 열고·이동하고·Esc 로 닫고·포커스가 돌아오는지 확인해라.

---

## 배치 C — `mobile.css` 이름 바로잡기 (워커 불필요)

내용이 전부 자격증명 모달 스타일인데 이름이 `mobile.css` 다.
`<link>` 에 `media` 속성이 없어 항상 로드되므로 **이름만 틀렸다.**

```bash
git mv css/mobile.css css/cred-modal.css
```

### 🔴 세 개를 한 커밋에 (지시서 #28 과 같은 함정)

```
① index.html 의 <link href="css/mobile.css">
② sw.js 의 ASSETS 목록          ← addAll 은 하나만 404 여도 전체 실패
③ sw.js 의 CACHE  v7 → v8
```

**나눠 커밋하면 그 사이 배포될 때 서비스워커가 죽는다.**
계약 테스트가 ASSETS 를 양방향으로 검사하므로 `npm test` 가 잡는다.

---

## 워커 호출 (지침서 §5 와 동일)

```json
{ "prompt": "...", "agent_id": "ag_deepseek", "report_to": "ag_hermes",
  "max_tokens": 1500, "trace_id": "ux-a-20260816" }
```

- 파일 이름만 주지 마라 — **코드를 붙여 넣어라**
- 한 번에 한 가지만. `truncated: true` 면 **2500 으로 재호출**
- `"확실하지 않으면 '확실하지 않음'이라고 답하라. 추측하지 마라."` 를 빼지 마라
- Kimi 는 복잡한 작업에 30~38초 걸린다. 정상이다

---

## 🔴 push 는 승인 후에

```
로컬 브랜치 작업 → 커밋 → 검증 → 보고 → 멈춤
                                      ↓
                        사용자가 확인하고 추가 지령
```

브랜치: `ux-batch-abc`. **보고까지 하고 멈춰라.**

---

## 보고 양식 (배치마다 따로)

```
[배치 ?] A / B / C
- 워커 호출 : __회
- truncated → 재호출 : __회
- 최종 판단 불가 : __개          ← 0 이어야 한다
- 채택 : __건 / __개 중
- 반려 : 취향 __ / 오판 __ / 환각 __
- 환각 원문 : ______             ← 요약하지 말고 그대로

- 합격 기준 숫자:
    A) Esc 안 되는 패널 __개 (0 기대) / 새 리스너 __개 (1 기대)
    B) 포커스 미이동 __개 · 복귀 안 됨 __개 · role 없음 __개 (전부 0 기대)
    C) npm test __스위트 __건 · CACHE v__

[결과] 승인대기 / 차단(사유)
```

---

## 하지 말 것

1. **Esc 리스너를 패널마다 만들지 마라** — 위임 하나
2. **ARIA 제안을 앵커만 보고 채택하지 마라** — 붙는 요소를 확인해라
3. **`sw.js` 를 다음 커밋으로 미루지 마라** — 배포 순간 죽는다
4. **A 와 B 를 병렬로 하지 마라** — 같은 파일이다
5. **인라인 style·색상 토큰화에 손대지 마라** — 이번 범위가 아니다 (지침서 §7)
6. **push 하지 마라** — 보고까지

---

`trace_id`: `ux-a-20260816` / `ux-b-20260816` / `ux-c-20260816`
정상 완료 → `report` / 판단이 안 서면 → `instruction`
