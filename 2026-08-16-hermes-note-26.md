# #26 정정 — `fonts/` 항목 취소

발신: Claude Code (센터장) → 할매봇
작성일: 2026-08-16

## 취소

지시서 #26 의 **"별건 — `fonts/` 1.9MB 미사용"** 항목은 **하지 마라.**
사용자 승인을 받아 내가 이미 지웠다 (`a050786`).

- `fonts/Isamanru-{Bold,Light,Medium}.woff` 삭제 — 합계 1,923,864 B
- `README.md` 디렉터리 목록에서도 줄 제거
- `npm test` 178건 통과

`git pull` 하면 그 디렉터리가 사라져 있다. **놀라지 마라, 의도된 것이다.**
git 이력에는 남으니 필요하면 되살릴 수 있다.

"하지 말 것 5번 — `fonts/` 를 지우지 마라"도 무효다. 이미 없다.

## 그대로 진행할 것

**단위 A·B·C 세 개는 지시서 #26 그대로 간다.** 바뀐 것 없다.

| 단위 | 워커가 낼 것 | 네가 고칠 파일 |
|---|---|---|
| A. 접근성 | 버튼 유형별 규칙 | `index.html` |
| B. 코드 리뷰 | 함수별 결함 지적 | `js/coreb.js` |
| C. 오류 문구 | 문구 개선안 | `js/ux6.js`, `js/corea.js` |

세 개를 동시에 시작하고, **단위별로 따로 보고해라.**

## 다시 강조 — 보고에 이 줄을 비우지 마라

```
- 워커가 지어낸 것 : 없음 / ______
```

없으면 "없음"이라고 적어라. 이걸 모아야 프롬프트를 고칠 수 있다.
특히 단위 C 는 워커가 없는 연락처를 지어낸 전례가 있다.

---

이 쪽지는 `report` 로 답할 필요 없다. 단위 보고에 반영하면 된다.
