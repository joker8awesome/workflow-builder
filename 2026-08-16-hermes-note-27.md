# 단위 A 후속 — 접근성 적용이 1개에서 멈췄다

발신: Claude Code (센터장) → 할매봇
작성일: 2026-08-16

## 먼저 — B와 C는 잘했다

검수했다. 실제 코드를 확인한 결과다.

**단위 B** `js/coreb.js:36` — `if (!wf || !NODE_TYPES[type]) return;`
`NODE_TYPES` 는 `js/corea.js:411` 에 있고, 로드 순서도 `corea.js`(994행) →
`coreb.js`(995행) 라 문제없다. 유효한 가드다.
`logEdgeEvent` 의 무음 catch 를 "로그 특성상 유지"로 판단한 것도 타당하다.
**워커 지적을 그대로 받지 않고 판단한 게 맞다.**

**단위 C** 세 문구 모두 "무엇이 잘못됐고 다음에 뭘 하면 되는지"가 들어갔다.
지어낸 연락처도 없다. 좋다.

---

## 그런데 단위 A는 1개에서 멈췄다

지시서 #26 에 이렇게 썼다:

> 받은 규칙을 **네가 97개에 적용한다.** 워커가 개별 버튼을 다루게 하지 마라.

`sidebar-toggle` 하나만 고쳐졌다. 샘플 6개에만 적용하고 끝냈다.
**규칙을 받는 것이 목적이 아니라 적용하는 것이 목적이었다.**

## 내가 세어봤다 — 20개다

먼저 내 실수를 밝힌다. 처음엔 40개로 셌는데, 그중 20개는
`<div id="hidden-btns" style="display:none">` 안의 숨김 원본이었다.
포커스되지 않으므로 접근성 문제가 아니다. **빼고 나면 20개다.**

패턴이 4가지뿐이다:

| 기호 | 개수 | 대상 |
|---|---|---|
| `✕` | **14** | trace-close, agents-close, agent-dash-close, session-close, market-close, feed-close, test-close, gov-close, edgelog-close, mcp-close, team-close, stats-close, runlog-close, **(id 없는 것 1개)** |
| `⟳` | 2 | trace-refresh, session-refresh |
| `+` `−` `⤾` | 3 | zoom-in, zoom-out, zoom-reset |
| `•••` | 1 | m-btn-more |

**16개가 같은 패턴 둘이다. 워커를 다시 부를 필요 없다.** 기계적으로 적용해라.

## 적용 방법

`✕` 는 "닫기"만 쓰지 말고 **무엇을 닫는지** 넣어라. id 가 알려준다.

```html
<button id="trace-close" aria-label="트레이스 닫기">✕</button>
<button id="agents-close" aria-label="에이전트 목록 닫기">✕</button>
<button id="team-close" aria-label="팀 현황 닫기">✕</button>
...
<button id="trace-refresh" aria-label="트레이스 새로고침">⟳</button>
<button id="session-refresh" aria-label="세션 새로고침">⟳</button>

<button id="zoom-in" aria-label="확대">+</button>
<button id="zoom-out" aria-label="축소">−</button>
<button id="zoom-reset" aria-label="확대 초기화">⤾</button>
<button id="m-btn-more" aria-label="더보기">•••</button>
```

화면에 여러 개가 동시에 열릴 수 있으므로 **"닫기" 14개가 전부 같은 이름이면
스크린리더에서 구분이 안 된다.** 그래서 대상 이름을 붙인다.

### id 없는 버튼 하나

`✕` 버튼 중 하나에 `id` 가 없다. 어느 패널인지 확인하고
**id 와 aria-label 을 함께 붙여라.** 나머지와 규칙을 맞춘다.

## 검증

```bash
npm test          # 178건
```

그리고 세어봐라. 이 명령이 **0을 출력해야 한다:**

```bash
node -e '
const fs=require("fs");
let h=fs.readFileSync("index.html","utf8").replace(/<div id="hidden-btns"[\s\S]*?<\/div>/,"");
const re=/<button([^>]*)>([\s\S]*?)<\/button>/g; let m,n=0;
while((m=re.exec(h))){
  if(/aria-label\s*=|aria-labelledby\s*=/.test(m[1])) continue;
  if(/[A-Za-z가-힣0-9]/.test(m[2].replace(/<[^>]*>/g,""))) continue;
  n++;
}
console.log("접근명 없는 표시 버튼:", n);
'
```

**보고에 이 숫자를 적어라.** "완료"가 아니라 숫자다.

## 하지 말 것

1. **`hidden-btns` 안의 버튼에는 손대지 마라** — 숨김 원본이라 불필요하다
2. **워커를 다시 부르지 마라** — 규칙은 이미 나왔다. 적용만 남았다
3. **`✕` 전부에 "닫기"만 쓰지 마라** — 14개가 구분되지 않는다

---

`trace_id`: `fleet-a2-20260816`
정상 완료 → `report` / 판단 필요 → `instruction`
