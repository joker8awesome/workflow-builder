# Phase 2 계획 — 프론트엔드 안전망

작성: Claude Code (센터장)
작성일: 2026-08-16
기준: `696635a`

---

## 0. 실측 — 무엇이 이미 닫혔나

계획을 세우기 전에 현재 상태를 측정했다. 추측한 것은 없다.

| 항목 | 상태 | 근거 |
|---|---|---|
| 인증 강제 | ✅ **켜져 있다** | 무인증 `POST /api/cache/get`, `/api/messages` → 둘 다 **401** |
| 미들웨어 없는 쓰기 라우트 | ✅ 2건뿐, 모두 의도된 것 | `/api/webhook/:token`(경로 토큰), `/api/telegram/webhook`(secret 헤더) |
| 프론트 키 전달 | ✅ 개인별 | `localStorage['wf_api_key']`, 자격증명 발급으로 취득. 공용 키 없음 |
| 배포본 | ✅ 분해 반영됨 | GitHub Pages 가 `js/corea.js` 등 참조 |
| 미사용 폰트 1.9MB | ✅ 삭제 | `a050786` |
| 접근성 (버튼) | ✅ 20 → 0 | aria-label 23개 전부 고유 |

**비용 노출·인증 문제는 닫혔다.** 이전 계획서(`2026-08-15-team-tool-plan.md`)의
"무인증 라우트 23개" 항목은 해소됐다. 다시 열지 않는다.

---

## 1. 남은 진짜 구멍 — 프론트엔드에 자동 검증이 0이다

```
백엔드·운영 검사   178건
프론트엔드 검사      0건
```

오늘 `index.html` 5,589줄에서 **JS 약 4,500줄을 16개 파일로 옮겼다.**
검증은 육안 6항목이 전부였다. 통과했지만, **다음에 무언가 깨지면 사용자가 먼저 안다.**

구체적인 위험이 이미 코드에 있다:

```js
js/ux2.js:274
document.getElementById('btn-group').addEventListener('click', toggleGroup);
```

**널 가드가 없다.** 이 id 가 `index.html` 에서 사라지거나 오타가 나면
그 줄에서 예외가 나고 **이후 스크립트가 통째로 멈춘다.**
분해가 정확히 이런 어긋남을 만드는 작업이었다.

---

## 2. 순서가 강제된다 — 이름부터

Phase 0 때와 같은 구조다. 먼저 하지 않으면 뒤가 무너진다.

```
2-1  파일 이름 정리 (직렬)     ← 먼저
      ↓  이름이 안 바뀐 뒤에야
2-2  프론트 계약 테스트
```

**이름을 안 고치고 테스트를 쓰면, 곧 바뀔 이름을 참조하는 테스트가 된다.**

### 이름이 실제로 문제를 일으켰다

| 파일 | 첫 줄이 말하는 내용 | 이름이 말하는 것 |
|---|---|---|
| `agent1.js` | 가상화 렌더링 — 뷰포트 밖 노드 | 에이전트? |
| `market.js` | `loadSessions()` | 마켓? |
| `tests.js` | 테스트 + **더보기 드롭다운** + 엣지 로그 | 테스트만? |
| `ux1`~`ux6` | 실행 엔진 / Undo · Redo / 터치 / LLM 노드 | 아무것도 안 알려줌 |

무의미한 게 아니라 **틀렸다.** 실제로 대가를 치렀다 —
지시서 #26 에서 단위 C 를 `ux6.js` 에 배정했는데 실제 문구는 `tests.js`·`ux1.js` 에 있었다.
내가 `coreb.js` 에 코드 리뷰를 배정할 때도 안에 뭐가 있는지 모르고 배정했다.

### 이름은 워커에게 짓게 하지 않는다

파일의 함수 목록을 붙이고 **"무슨 일을 하는 파일인지 2~3단어로"** 만 받는다.
측정에서 요약·구조화 추출은 강점으로 확인됐다.

**파일명 자체를 제안하게 하지 마라.** 빈칸을 그럴듯한 것으로 채우는 성향이 있고,
이름은 빈칸이 아니라 판단이다. 설명을 받아 **내가/할매봇이 이름을 정한다.**

### 이름 바꿀 때 물리는 곳 둘

**① `sw.js` ASSETS 와 `<script src>` 는 같은 커밋에서 바꾼다**

`cache.addAll()` 은 목록 중 **하나라도 404 면 전체가 실패한다.** 부분 실패가 아니다.
서비스워커 설치가 통째로 죽는다.

**② `CACHE` 를 `wf-builder-v6` → `v7` 로 올린다**

안 올리면 기존 사용자의 서비스워커가 **옛 파일 목록을 계속 서빙한다.**

함수 이름은 전부 전역이고 바뀌지 않으므로, **파일 간 참조 수정은 없다.**
이름 바꾸기는 진짜로 기계적이다.

---

## 3. 프론트 계약 테스트 (2-2)

DB·네트워크 없이 도는 정적 검사만 넣는다. 기존 11스위트와 같은 방식이다.

| # | 검사 | 막는 사고 |
|---|---|---|
| 1 | `js/*.js` 의 모든 `getElementById('x')` 에 대응하는 `id="x"` 가 `index.html` 에 있다 | **로드 시 예외로 스크립트 전체 정지** |
| 2 | `index.html` 의 `<script src>` 순서가 의존 방향과 일치 (`corea` → `coreb`) | `NODE_TYPES` 미정의 |
| 3 | `sw.js` ASSETS 가 실제 파일 목록과 일치 | `addAll` 실패로 SW 설치 불가 |
| 4 | `id=` 중복 없음 | `getElementById` 가 엉뚱한 요소를 집음 |
| 5 | 표시 버튼에 접근명이 있다 (카운트 0) | 접근성 회귀 |
| 6 | `aria-label` 이 서로 다르다 | 닫기 14개가 구분 안 됨 |

5·6 은 오늘 손으로 확인한 것을 **고정하는** 것이다. 다시 손으로 세지 않는다.

1번이 가장 값싸고 가장 크다. 의존성이 없고, 분해가 깨뜨리는 바로 그 지점을 친다.

---

## 4. 미루는 것

**PWA manifest 동적 생성** (`js/ux5.js:104`) — JS 가 실패하면 설치가 안 된다.
실사용에 지장이 없어 이번엔 넣지 않는다. **묻어두는 게 아니라 미루는 것이다.**

---

## 5. 합격 기준

각 단위는 **숫자로 판정한다.** "완료"는 판정이 아니다.

```
2-1  이름 정리 : 남은 무의미 이름 0개 · npm test 178건 · 화면 회귀 6항목
2-2  계약 테스트 : 12스위트 · 검사 178 + N건 · 일부러 깨뜨렸을 때 실제로 실패하는지 확인
```

**2-2 에는 조건이 하나 더 있다.** 새 테스트가 **진짜로 잡는지** 확인해야 한다.
`getElementById` 하나를 일부러 오타 내고 테스트가 빨간불이 되는지 본 뒤 되돌린다.
통과만 확인하면 아무것도 검사하지 않는 테스트를 통과로 오인한다 —
오늘 하루 내내 닫아온 "선언은 있고 구현은 없음"이 테스트에서도 똑같이 난다.
