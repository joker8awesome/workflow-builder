# 커멘드센터 웹 고도화 — 워커 운용 지침

작성: Claude Code (센터장)
작성일: 2026-08-16
기준 커밋: `aef6d35`
워커: `moonshotai/kimi-k3` (`ag_deepseek`)

이 문서는 **상시 참조용**이다. 개별 지시서는 여기를 가리키고 배치만 지정한다.

---

## 0. 진단 — 전부 실측이다

오늘 분해·이름정리·접근성 작업 뒤의 상태를 직접 세었다. 추측한 것은 없다.

| # | 발견 | 실측값 | 성격 |
|---|---|---|---|
| 1 | **Esc 로 닫히는 패널이 없다** | 패널 15개 / Escape 핸들러 2개 (둘 다 다른 용도) | 🔴 반복 패턴 |
| 2 | **포커스가 이동·복귀하지 않는다** | 포커스 관련 코드 5줄 / `role="dialog"` 1개 | 🔴 반복 패턴 |
| 3 | `css/mobile.css` 가 모바일 CSS 가 아니다 | 내용은 자격증명 모달 전용, 조건 없이 `<link>` | 🟠 이름 오류 |
| 4 | 인라인 style 이 많다 | `index.html` 126곳 | 🟡 토큰화 대상 |
| 5 | 하드코딩 색상 | `index.html` 31종, `mobile.css` 15종 | 🟡 토큰화 대상 |
| 6 | 반응형 분기가 얕다 | `main.css` 미디어쿼리 2개 (900px·768px) | 🟡 |

### 왜 1·2 를 먼저 하나

**패턴이 반복되고, 성공을 숫자로 셀 수 있다.**

`#27`(접근명 없는 버튼 20개)이 같은 형태였다 — 워커가 샘플로 규칙을 만들고,
할매봇이 목록 전체에 적용하고, 카운트가 0 이 되는지로 판정했다. 그 방식이 통했다.

4·5·6 은 크고 기계적이지만 **워커의 강점이 아니다.** 토큰 치환은 판단이 아니라
일괄 수정이다. **지금은 미룬다 — 묻어두는 게 아니라 미루는 것이다.**

---

## 1. 배치 A — Esc 로 닫기 (패널 15개)

### 현재

```js
// 있는 Escape 핸들러 2개는 패널과 무관하다
js/groups-export-ws.js:88   if (e.key === 'Escape') { input.value = cur; input.blur(); }   // 인라인 편집 취소
js/virtual-render-palette.js:37  if (e.key === 'Escape') pal.style.display = 'none';       // 팔레트
```

패널을 열고 Esc 를 누르면 **아무 일도 일어나지 않는다.** 마우스로 ✕ 를 찾아야 한다.

### 목표

**핸들러 15개를 만들지 마라.** 위임 방식 하나로 처리한다 —
가장 위에 열린 패널을 닫는 `keydown` 리스너 하나.

### 워커에게 물을 것

패널 마크업 샘플 2개와 기존 토글 함수를 붙이고, **위임 처리 방식**을 물어라.
"패널마다 리스너를 달라"는 답이 오면 반려한다 — 15개를 개별로 달면
나중에 패널이 늘 때마다 빠뜨린다.

### 합격 기준

```
Esc 로 닫히지 않는 패널 : 0개
새로 만든 keydown 리스너 : 1개    ← 15개면 실패다
```

---

## 2. 배치 B — 포커스 이동과 복귀 (패널 15개)

### 현재

```
포커스 관련 코드   5줄
role="dialog"      1개
```

패널을 열어도 포커스가 그대로 뒤에 남는다. 키보드 사용자는 Tab 을 수십 번 눌러야
패널에 닿는다. 닫아도 포커스가 어디로 갈지 모른다.

### 목표

- 패널이 열리면 패널 안으로 포커스를 옮긴다
- 닫으면 **열었던 버튼으로 되돌린다**
- 패널에 `role="dialog"` 와 `aria-modal="true"` 를 준다

### 🔴 여기서 워커가 틀리기 쉽다

ARIA 속성은 **어느 요소에 붙느냐가 핵심**인데, 앵커 검사로는 그걸 못 잡는다.
앵커는 진짜인데 붙이는 위치가 틀릴 수 있다.

**실제로 겪었다.** 딥시크가 `aria-expanded` 를 패널에 붙이라고 했고, 나는 그걸
"기술적으로 정확하다"고 적었다. 틀렸다 — 그건 **여닫는 버튼**에 붙는다.
모델을 바꿔 다시 재보고서야 알았다. **운이 좋았던 것이지 절차가 잡은 게 아니다.**

그래서 §4 에 규칙을 하나 더했다.

### 합격 기준

```
포커스가 안 옮겨지는 패널 : 0개
닫을 때 복귀 안 되는 패널 : 0개
role="dialog" 없는 패널   : 0개
```

---

## 3. 배치 C — `css/mobile.css` 이름 바로잡기 (워커 불필요)

내용이 전부 자격증명 모달 스타일인데 이름이 `mobile.css` 다.
`<link>` 에 `media` 속성이 없어 항상 로드되므로, **이름만 틀렸다.**

`css/cred-modal.css` 로 바꾼다. **`#28` 과 같은 함정이 있다:**

```
① index.html 의 <link>
② sw.js 의 ASSETS      ← addAll 은 하나만 404 여도 전체가 실패한다
③ sw.js 의 CACHE v7 → v8
```

**세 개가 한 커밋에 없으면 배포 순간 서비스워커가 죽는다.**
계약 테스트가 ASSETS 를 양방향으로 검사하므로 `npm test` 가 잡는다.

---

## 4. 환각 게이트 — 규칙 6개

`2026-08-16-deepseek-worker-capability.md` 부록에서 Kimi 로 재측정했다.
**이름만으로 지어내는 위험은 줄었지만, 거짓 전제를 받아들이는 구멍은 그대로다.**

| # | 규칙 | 근거 |
|---|---|---|
| ① | 제안은 실제 코드에서 복사한 **앵커**를 포함. `grep` 실패 시 즉시 반려 | 형식 준수는 확인됨 |
| ② | 저장소에 없는 파일·함수·설정을 언급하면 반려 | **거짓 전제 방어의 핵심** |
| ③ | 적용 후 `npm test` 통과가 합격 기준. 워커 자기 평가는 근거가 아니다 | |
| ④ | 반려 3회면 회수하고 센터장에게 에스컬레이션 | 무한 재검토는 큐를 태운다 |
| ⑤ | **지시를 보내기 전에 지시 자체의 사실을 확인** | 없는 헬퍼를 "있다"고 했더니 Kimi 도 그걸 썼다 |
| ⑥ | **ARIA·포커스 제안은 속성이 붙는 요소가 맞는지 확인** | 앵커는 진짜인데 위치가 틀릴 수 있다 |

### ⑥ 이 새로 필요한 이유

앵커 게이트는 **형식만 본다.** ARIA 는 형식이 아니라 의미다.
알아둘 것 두 가지 (Kimi 는 둘 다 맞췄지만 확인은 해라):

```
aria-expanded          → 여닫는 버튼에 (패널 아님)
role="dialog"          → 대화상자 컨테이너에
aria-modal="true"      → 같은 컨테이너에
```

---

## 5. 워커 호출 형식

```
아래는 js/<파일명> 의 해당 부분이다.

<실제 코드 붙여넣기>

<한 가지 작업 지시>

형식:
앵커: <위 코드에서 그대로 복사한 한 줄>
제안: <설명>

위 코드에 없는 함수·파일·설정은 언급하지 마라.
확실하지 않으면 "확실하지 않음"이라고 답하라. 추측하지 마라.
```

```json
{ "prompt": "...", "agent_id": "ag_deepseek", "report_to": "ag_hermes",
  "max_tokens": 1500, "trace_id": "..." }
```

- **파일 이름만 주지 마라.** 워커는 파일을 못 본다
- **한 번에 한 가지만.** 응답이 `truncated: true` 면 2500 으로 **재호출**한다.
  잘린 답을 채택 판단에 쓰지 마라
- **"추측하지 마라" 줄을 빼지 마라.** 모델이 또 바뀔 수 있다
- 복잡한 작업은 30~38초 걸린다. 정상이다

---

## 6. 보고

배치마다 따로 보고한다. 묶으면 어디서 틀어졌는지 못 찾는다.

```
[배치 ?] A / B / C
- 워커 호출 : __회
- truncated → 재호출 : __회
- 최종 판단 불가 : __개        ← 0 이어야 채택률이 유효하다
- 실제 결함/채택 : __건 / __개 중
- 반려 : 취향 __ / 오판 __ / 환각 __
- 환각 원문 : ______           ← 요약하지 말고 그대로
- 합격 기준 숫자 : ______      ← "완료" 가 아니라 숫자
- npm test : __스위트 / __건
```

**push 는 승인 후에 한다.** 로컬 브랜치에서 작업하고 보고까지 하고 멈춘다.
사용자가 반영 사항을 확인한 뒤 추가 지령을 준다.

---

## 7. 미루는 것 (묻어두는 게 아니다)

| 항목 | 이유 |
|---|---|
| 인라인 style 126곳 토큰화 | 크고 기계적. 워커 강점 아님 |
| 하드코딩 색상 46종 | 위와 같음 |
| 반응형 분기 확장 | 실기기 확인이 필요. 정적 판단으로 부족 |
| PWA manifest 동적 생성 | 실사용 지장 없음 |

배치 A·B 가 끝나고 채택률이 나오면 다시 판단한다.
