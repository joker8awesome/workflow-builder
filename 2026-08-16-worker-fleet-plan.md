# 워커 배치로 커멘드센터 웹 고도화 — 계획

작성: Claude Code (센터장)
작성일: 2026-08-16
대상: `index.html` (5,589줄 / 281KB), 커멘드센터 웹

---

## 0. 먼저 — 워커가 실제로 할 수 있는 일

계획을 세우기 전에 코드를 확인했다. 전제가 틀리면 계획 전체가 무너지므로 여기부터 적는다.

| 확인 항목 | 근거 | 결과 |
|---|---|---|
| 딥시크가 코드 패치를 낼 수 있나 | `server.js:1699` `max_tokens: 800` | ❌ 불가 |
| 딥시크가 파일을 읽고 쓰나 | `/api/llm/worker` = prompt → LLM → 텍스트 | ❌ 없음 |
| 할매봇이 워커에 지시할 수 있나 | `TEAM_ROUTES` 는 `GET /api/team/next` 에서만 읽힘 — 권고일 뿐 강제 아님 | ✅ 가능 |
| 워커 보고가 할매봇에게 닿나 | `server.js:1705` `to_agent='ag_orch'`, `status='sent'` | ❌ **닿지 않음** |

### 그래서 워커 함대의 성격이 정해진다

**딥시크는 코딩 워커가 아니라 검토·분석 워커다.**

800 토큰은 한국어로 대략 600자다. 리뷰 의견, 테스트 케이스 목록, 문구 초안,
접근성 점검표에는 충분하지만 **5,589줄 파일에 대한 diff 를 담을 수 없다.**
파일 접근도 없어서 스스로 적용하지 못한다.

따라서 역할 분담은 이렇게 된다:

```
딥시크 워커  →  제안만 낸다 (텍스트)
할매봇      →  검수하고, 직접 적용하고, 테스트를 돌린다
센터장(나)  →  계획을 세우고, 잘못된 방향을 다시 잡는다
```

사용자가 우려한 환각 문제는 **구조적으로 이미 절반이 막혀 있다** — 워커는 아무것도
적용할 수 없다. 남은 위험은 "그럴듯한 거짓 제안을 할매봇이 믿고 적용하는 것"이고,
그건 아래 §4 의 게이트로 막는다.

---

## 1. 🔴 착수 전에 고쳐야 할 것 — 워커 보고가 사라진다

`/api/llm/worker` 가 결과를 이렇게 기록한다:

```js
INSERT INTO agent_messages (msg_type, from_agent, to_agent, payload, status, trace_id)
VALUES ('report', $1, 'ag_orch', $2, 'sent', $3)
```

문제가 둘이다.

1. **`to_agent = 'ag_orch'`** — 할매봇(`ag_hermes`)이 아니다. 지시한 쪽과 받는 쪽이 다르다
2. **`status = 'sent'`** — `list_pending` 은 `'pending'` 만 조회한다. 오늘 `POST /api/messages`
   에서 고친 것과 **같은 버그가 여기 남아 있다**

즉 지금 워커를 돌리면 **결과가 큐에서 사라진다.** 할매봇은 응답 본문(HTTP)으로만
결과를 볼 수 있고, 큐 기록은 아무도 읽지 못한 채 쌓인다.

**조치:** 호출자가 `report_to` 를 지정할 수 있게 하고, 기본값을 호출한 에이전트로,
상태를 `'pending'` 으로 바꾼다. 이건 내가 처리한다 (§5 Phase 0).

---

## 2. 구조적 제약 — 병렬 작업을 막는 것

`index.html` 은 **한 파일에 5,589줄, 281KB** 다. HTML·CSS·JS 가 모두 들어 있다.

**워커를 여럿 붙여도 이 파일은 병렬 편집이 불가능하다.** 같은 파일에 대한 여러 diff 는
충돌하고, 281KB 짜리 움직이는 표적에 대한 패치는 만들자마자 낡는다.

그래서 순서가 강제된다:

```
Phase 0  (직렬, 할매봇 단독)   파일 분해 — 여기서 병렬이 가능해진다
   ↓  이게 끝나기 전에는 아무도 병렬로 못 간다
Phase 1+ (병렬, 워커 배치)     분해된 파일별로 한 명씩
```

Phase 0 을 건너뛰고 팬아웃하면, 충돌하는 diff 를 하루 종일 화해시키게 된다.

---

## 3. 작업 분해

### Phase 0 — 선행 (직렬, 할매봇, 워커 미투입)

| # | 작업 | 완료 조건 |
|---|---|---|
| 0-1 | `/api/llm/worker` 보고 경로 수정 (§1) | 워커 보고가 `list_pending` 에 뜬다 |
| 0-2 | `index.html` 분해 — `css/`, `js/` 로 분리 | 파일당 1,000줄 이하, 화면 동작 동일 |
| 0-3 | 분해 후 회귀 확인 | `npm test` 173건 + 주요 화면 육안 확인 |

**0-2 가 이 계획 전체의 관문이다.** 분해 경계는 이미 코드에 있다 — 패널별 렌더 함수,
`togglePanel`, `loadTeamStatus` 등. 새 구조를 발명하지 말고 있는 경계를 그대로 파일로 옮긴다.

### Phase 1 — 병렬 (파일당 워커 1명)

분해가 끝나야 확정되지만, 예상 작업 단위는 이렇다:

| 단위 | 내용 | 딥시크가 낼 것 | 할매봇이 할 것 |
|---|---|---|---|
| A | 접근성 — `aria-*`, 키보드 이동, 포커스 표시 | 요소별 점검표·수정 제안 | 적용·검증 |
| B | 서비스워커 — 안 쓰는 폰트 1.9MB 캐시 제거 | 캐시 목록 검토 의견 | 적용·용량 측정 |
| C | 오류 표시 통일 — `panelErrorHtml` 적용 누락 지점 | 누락 지점 목록 | 적용 |
| D | 문구·레이블 정리 | 문구 초안 | 채택·적용 |
| E | 프론트 계약 테스트 | 테스트 케이스 목록 | 테스트 작성 |

**한 워커 = 한 파일.** 두 워커가 같은 파일을 만지는 배치는 만들지 않는다.

---

## 4. 환각 게이트 — 할매봇이 기계적으로 검사한다

워커에게 "스스로 검증하라"고 시키지 않는다. 그건 오늘 하루 내내 닫아온
"선언은 있고 구현은 없음" 과 같은 실패다. **검사는 전부 할매봇이, 기계적으로 한다.**

### 규칙 4가지

**① 앵커 — 실제 파일에서 복사한 문자열을 반드시 포함**

모든 제안은 대상 파일의 실제 코드 한 줄 이상을 그대로 옮겨 적어야 한다.
할매봇은 그 문자열을 `grep` 한다. **없으면 즉시 반려한다. 디버깅하지 않는다.**

```
제안 형식:
  파일: js/panels.js
  앵커: function togglePanel(id) {        ← grep 으로 존재 확인
  제안: 이 함수에 aria-expanded 갱신 추가
```

앵커가 안 맞는다는 건 워커가 파일을 보지 않고 지어냈다는 뜻이다. 그 제안의 나머지도
믿을 수 없다.

**② 존재하지 않는 것을 발명하면 반려**

파일 경로, 함수 이름, API 엔드포인트, 설정 키 — 저장소에 없는 것을 언급하면 반려.
할매봇이 `grep` 으로 확인한다.

**③ 적용 후 `npm test` 통과가 합격 기준**

워커의 자기 평가는 근거가 아니다. **173건이 통과해야 합격이다.**
실패하면 되돌리고 반려한다.

**④ 반려는 3회까지 — 그 다음은 나에게 올린다**

무한 재검토 루프는 큐를 태운다. `MAX_TRIES` 와 같은 이유다.
3회 반려되면 그 작업 단위는 **워커에게서 회수하고 나에게 에스컬레이션**한다.

### 작업 단위마다 보고

한 단위가 끝날 때마다 할매봇에게 보고한다. 묶어서 한 번에 보고하지 않는다 —
어디서 틀어졌는지 못 찾는다.

---

## 5. 프로토콜 — 할매봇 ↔ 센터장

### 🔴 가장 중요한 한 가지

**나를 깨우려면 `type` 이 `instruction` 이어야 한다.**

내 `ops/poll-queue.js` 는 이렇게 동작한다:

```js
report            → ops/inbox.md 에 적고 claim. 세션 안 뜬다
instruction/command → 세션을 띄운다
```

즉 **`report` 로 보낸 에스컬레이션은 나를 깨우지 못한다.** 파일에 조용히 쌓일 뿐이다.
이건 소리 없이 실패하는 종류라 반드시 지켜야 한다.

| 상황 | type | 이유 |
|---|---|---|
| 단위 완료, 정상 진행 | `report` | 내가 볼 필요는 있지만 깨울 필요는 없다 |
| **워커가 3회 반려됨** | `instruction` | 방향을 다시 잡아야 한다 |
| **계획이 현실과 다름** | `instruction` | 계획을 고쳐야 한다 |
| **`npm test` 가 깨짐** | `instruction` | 즉시 판단 필요 |
| 애매하면 | `instruction` | 잘못 깨우는 비용 < 놓치는 비용 |

### trace_id

한 작업 단위는 하나의 `trace_id` 를 끝까지 유지한다.
`fleet-<단위>-<날짜>` 형식. 예: `fleet-a11y-20260816`.

### 왕복 예산

실측 왕복은 **약 3분** (`msg_180` 22:57 적재 → `msg_181` 3분 뒤 보고).
1분 cron 이므로 지시 하나당 3~5분으로 잡는다.

**작업을 묶어서 보내라.** 20번 왕복하는 설계는 만들지 않는다.
Phase 1 은 단위 A~E 를 한 번에 배분하고, 단위별로 보고만 따로 받는다.

---

## 6. 승인이 필요한 것

기존 승인 범위 그대로다: `deploy`, `credential.issue`, `rollback`.

워커 작업 중 이 셋에 해당하는 건 **Phase 0-2 분해 결과 배포** 하나다.
나머지는 자동 통과한다.

---

## 7. 순서 요약

```
Phase 0   0-1 워커 보고 경로 수정  (내가 함)
          0-2 index.html 분해      (할매봇, 직렬)   ← 관문
          0-3 회귀 확인
             ↓  여기까지 끝나야
Phase 1   단위 A~E 병렬 배분 (할매봇 → 워커들)
          단위별 보고 → 환각 게이트 → 적용 → npm test
             ↓
Phase 2   분해 결과를 본 뒤 재계획
```

**Phase 1 의 구체 배분은 Phase 0-2 가 끝난 뒤 확정한다.** 분해 결과를 보지 않고
파일명을 미리 정하면, 그게 바로 §4 ② 가 반려하는 "존재하지 않는 것"이 된다.

---

## 부기

DB `agents.machine.spec` 이 `"Mac (Claude Desktop)"` 인데 실제는 Windows + Claude Code 다.
할매봇이 갱신을 제안했다. 급하지 않으니 이번 작업과 분리해 두었다 — 지시하면 처리하겠다.
