# 할매봇 작업 지시서 #33 — 코드 리뷰 2차 (네트워크 경계 12개)

발신: Claude Code (센터장)
수신: **할매봇 (VPS)**
작성일: 2026-08-17
기준 커밋: `cd73a1e`
상시 지침: `2026-08-16-web-ux-directive.md` §4~6

> **`#30` 을 대체한다.** 그때는 워커가 딥시크였고 실행 타임아웃이 10분이었다.
> 둘 다 바뀌었으니 전제부터 새로 적는다. `#30` 은 무시해라.

---

## 그동안 바뀐 것 세 가지

**① 워커가 Kimi 다** (`moonshotai/kimi-k3`)

같은 프로브 14개를 다시 돌렸다 (`2026-08-16-deepseek-worker-capability.md` 부록).

- **이름만으로 지어내는 위험이 줄었다.** 코드 없이 물으면 거부한다
- **코드 리뷰가 더 깊다.** `queue-trigger.js` seen 버그에서 재시도 유실은 물론
  예외 미처리와 원자성 문제까지 짚었다 — 내가 직접 검토했을 때 못 본 것이다
- **거짓 전제는 여전히 받아들인다.** 없는 헬퍼를 "있다"고 하면 그걸 쓰는 제안을 낸다.
  게이트 ②·⑤ 를 느슨하게 하지 마라

**② 세션이 30분까지 돌 수 있다**

`#32` 때 세션 2개가 hang 한 원인을 네가 찾아줬다 — `spawnSync ETIMEDOUT`.
`queue-trigger.js` 의 실행 타임아웃이 10분이었는데 배치 A·B·C 가 19분 걸렸다.
게다가 재시도 정책이 죽이기를 3회 반복했다. **고장이 아니라 일하는 중이었다.**

```
타임아웃    10분 → 30분 (WF_TRIGGER_TIMEOUT_MS)
잠금 회수   타임아웃 + 5분으로 연동
```

**이번 배치는 끊기지 않고 완주한다.** 13회 호출에 10~15분 예상된다.

**③ 절단이 눈에 보인다**

`max_tokens` 를 요청별로 지정할 수 있고, 잘리면 응답에 `truncated: true` 가 온다.
`#29` 때 12건 중 7건이 잘렸는데 그걸 알 방법이 없어 채택률이 무효였다.

---

## 왜 이 12개인가

`#29`(저장·실행취소 12개)에서 나온 진짜 결함 두 개가 **둘 다 네트워크 경계**였다.

```
syncToServer        fetch 는 4xx·5xx 에 예외를 안 던진다. r.ok 를 안 봤다
flushOfflineQueue   동기화 완료 전에 큐를 지웠다
```

**같은 광맥을 판다.** 서버와 말을 주고받는 함수들이다.

### 대상 (전부 실재 확인함, `cd73a1e` 기준)

| 파일 | 함수 | 줄 | 맥락으로 넣을 질문 |
|---|---|---|---|
| `core-store.js` | `checkServer` | 8 | 서버 생사를 판단한다. 응답이 500이면 살아있다고 보나? |
| | `loadFromServer` | 26 | 서버 데이터로 로컬을 덮는다. 응답이 비었거나 깨졌으면? |
| | `loadTplFromServer` | 16 | 템플릿을 받는다. 실패하면 화면은 어떻게 되나? |
| | `updateSyncStatus` | 23 | 동기화 상태를 표시한다. 실제 상태와 어긋날 수 있나? |
| `exec-status.js` | `runNodeAction` | 24 | 노드 액션을 실행한다. 실패를 어떻게 알리나? |
| | `broadcastLocalChange` | 9 | 다른 탭에 변경을 알린다. 실패하면? |
| | `patchWSHandler` | 35 | WebSocket 메시지를 처리한다. 잘못된 메시지가 오면? |
| | `executeWorkflow` | **112** | 워크플로우를 실행한다. 중간 노드가 실패하면 나머지는? |
| `tests-more-menu.js` | `loadTests` | 9 | 테스트 목록을 받는다. 실패하면 빈 목록인가 오류인가? |
| | `runRegressionGate` | 31 | 회귀 게이트를 돌린다. 일부만 실패하면 판정은? |
| | `sendAgentCommand` | 17 | 에이전트에 명령을 보낸다. 도달 확인을 하나? |
| | `refreshMCPStatus` | 10 | MCP 상태를 갱신한다. 응답을 검사하나? |

**맥락 한 줄이 결과를 크게 바꾼다.** 위 질문을 그대로 프롬프트에 넣어라.

### `executeWorkflow` 는 나눠라

112줄이라 통째로 넣으면 잘린다. 앞부분(실행 준비·노드 순회)과
뒷부분(결과 처리·상태 갱신)으로 **두 번**, 각각 `max_tokens: 2500`.

---

## 호출 형식

```json
{ "prompt": "...", "agent_id": "ag_deepseek", "report_to": "ag_hermes",
  "max_tokens": 1500, "trace_id": "review2-20260817" }
```

```
아래는 js/<파일명> 의 함수다.

<함수 본문 그대로>

맥락: <위 표의 질문>

이 함수가 조용히 실패할 수 있는 지점을 한 문단으로 지적하라.
그런 지점이 없으면 "문제 없음"이라고만 답하라.

위 코드에 없는 함수·변수·파일은 언급하지 마라.
확실하지 않으면 "확실하지 않음"이라고 답하라. 추측하지 마라.
```

### 🔴 "문제 없음"이 정상 답변이다

12개 중 상당수는 문제가 없을 것이다. **그게 정상이다.**
문제를 찾으라고만 하면 모델은 없는 문제도 만들어낸다.
`#29` 에서 지적이 2건만 나온 것은 실패가 아니었다.

### 🔴 잘린 답을 채택 판단에 쓰지 마라

응답에 `truncated: true` 가 오면 **그 함수만 `max_tokens: 2500` 으로 재호출**해라.
`#29` 채택률(2/12=17%)이 무효였던 이유가 이것이다.

---

## 채택 판단 — 네가 한다

| 분류 | 처리 |
|---|---|
| **실제 결함** — 재현 경로가 그려진다 | 고친다 |
| **취향** — "이렇게 쓰는 게 낫다" 수준 | 반려 |
| **오판** — 코드를 잘못 읽었다 | 반려 |
| **환각** — 붙여넣지 않은 것을 언급 | 반려. **원문 그대로 기록** |

`#29` 에서 `logEdgeEvent` 무음 catch 를 "로그 특성상 유지"로 판단한 것,
**그게 맞는 판단이었다.** 워커 지적을 전부 고치지 마라.

고쳤으면 `npm test` **202건** 확인.

---

## 🔴 push 는 승인 후에

브랜치 `review-2`. **보고까지 하고 멈춰라.**
사용자가 확인한 뒤 추가 지령을 준다.

---

## 보고 양식

```
[1] 워커
- 호출 : __회        (13 기대 = 11개 + executeWorkflow 2회)
- truncated : __개 → 재호출 __회
- 최종 판단 불가 : __개      ← 0 이어야 채택률이 유효하다
- 세션 중단 : 없음 / ______  ← 30분 타임아웃이 충분했는지

[2] 채택 (분모는 판단 완료된 함수 수)
- 실제 결함 (고침) : __건 / __개 중
    · ______
- 반려 : 취향 __ / 오판 __ / 환각 __
- 환각 원문 : ______         ← 요약하지 말고 그대로

[3] 검증
- npm test : __스위트 / __건   (12/202 기대)
- 브랜치 : review-2
- 로컬 커밋 : ______
- push : 안 함 (승인 대기)

[결과] 승인대기 / 차단(사유)
```

**`[1]` 의 "최종 판단 불가 0"과 `[2]` 의 네 숫자가 이번 산출물이다.**
나머지 함수들을 계속 돌릴지 그 숫자로 정한다.

---

## 하지 말 것

1. **잘린 답을 채택 판단에 쓰지 마라** — 재호출
2. **12개 밖의 함수를 하지 마라**
3. **"문제 없음" 유도 문구를 빼지 마라**
4. **워커 지적을 전부 고치지 마라**
5. **push 하지 마라** — 보고까지

---

`trace_id`: `review2-20260817`
정상 완료 → `report` / 판단이 안 서는 지적 → `instruction`
