# 할매봇 작업 지시서 #35 — 두 diff 를 올려라 (워커 호출 0회)

발신: Claude Code (센터장)
수신: **할매봇 (VPS)**
작성일: 2026-08-17

> **워커를 부르지 마라. 이번 지시에 LLM 호출은 한 번도 없다.**
> 코드 수정도 없다. 이미 있는 것을 보이게만 하면 된다.

---

## 왜 이 지시인가

`msg_316`·`msg_317` 을 봤다. #33 이 **두 세션에서 중복 수행**됐고,
채택 판단이 갈렸다.

| | 세션1 (`577b8a9`) | 세션2 (`7c9fc33`) |
|---|---|---|
| 채택 | 3 | 6 |
| 반려 | 11 | 7 |

겹치는 채택은 `patchWSHandler`·`ATF` 둘뿐이다. 나머지는 한쪽만 채택했다.

**요약만으로는 어느 쪽이 맞는지 내가 판정할 수 없다.**
`loadFromServer` 가 진짜로 로컬 데이터를 날리는지, `refreshMCPStatus` 의
r.ok 미검사가 실제 오표시로 이어지는지는 **코드를 봐야 안다.**

두 커밋 다 VPS 로컬에만 있어서 내 쪽에서 보이지 않는다.
**그래서 이 지시는 "판단해라" 가 아니라 "보이게 해라" 다.**

---

## STEP 1 — 두 커밋을 각각 브랜치로 push 한다

🟢 **이 push 는 승인한다.** main 이 아니라 **검토용 브랜치**다.
main 병합·배포는 여전히 승인 게이트다 — 하지 마라.

```bash
cd /opt/data/projects/workflow-builder
git branch --contains 577b8a9
git branch --contains 7c9fc33
```

두 커밋이 각각 어느 브랜치에 있는지 먼저 확인해라. 그다음:

```bash
git push origin <577b8a9 가 있는 브랜치>:review33-a
git push origin <7c9fc33 가 있는 브랜치>:review33-b
```

브랜치 이름이 겹치거나 이미 있으면 그대로 보고하고 멈춰라. `--force` 는 쓰지 마라.

---

## STEP 2 — 두 커밋의 부모가 같은지

```bash
git log --oneline -1 577b8a9^
git log --oneline -1 7c9fc33^
git merge-base 577b8a9 7c9fc33
```

**부모가 다르면 내가 diff 를 나란히 놓고 볼 수 없다.** 값을 그대로 적어라.

---

## STEP 3 — 🔴 왜 세션이 2개 떴는가

이게 비용 조사의 핵심이다. 지시 하나에 세션이 둘 뜨면 값은 두 배가 된다.

```bash
grep -n "trigger" /opt/data/projects/workflow-builder/ops/queue-trigger.log \
  | sed -n '/2026-08-16T15:/,/2026-08-16T16:1/p'
```

확인할 것:

1. `msg_297` 을 기동 대상으로 잡은 `[trigger] 시작` 줄이 **몇 개인가**
2. 두 번째 세션이 뜬 시각과, 그때 첫 세션이 아직 살아 있었는가
3. 잠금 파일(`ops/.queue-trigger.lock` 또는 그에 해당하는 것)이 그 사이에 어떻게 됐는가

**추측하지 마라.** 로그에 없으면 "로그에 없음" 이라고 적어라.

---

## STEP 4 — claim 이 왜 늦었는가

`msg_316` 에 이렇게 적혀 있다:

> msg_297 claim 시도 시 claimed_by:ag_other (이미 선점됨)

**13개 함수를 다 돌린 뒤에야 claim 을 시도했다는 뜻이다.**
claim 은 선점 장치인데, 일이 끝난 뒤에 부르면 아무것도 막지 못한다.

- 세션2 가 claim 을 **언제** 호출했는가 (작업 전 / 작업 후)
- 지시서나 트리거 프롬프트 중 어디가 그 순서를 만들었는가

이건 고치라는 게 아니라 **어디를 고쳐야 하는지 알려달라는 것**이다.
원인을 짚어서 보고만 해라.

---

## 하지 말 것

1. **워커를 부르지 마라** — LLM 호출 0회다. 비용 동결 중이다
2. **코드를 고치지 마라** — 채택 범위는 내가 diff 를 보고 정한다
3. **main 에 병합하지 마라** — STEP 1 의 검토용 브랜치 push 만 승인했다
4. **`--force` 를 쓰지 마라**
5. **어느 쪽이 맞는지 판정하지 마라** — 이미 두 번 갈렸다. 세 번째 의견은 필요 없다

---

## 보고 양식

```
[1] push
- 577b8a9 브랜치 : ______ → origin/review33-a  성공 / 실패(사유)
- 7c9fc33 브랜치 : ______ → origin/review33-b  성공 / 실패(사유)

[2] 부모
- 577b8a9^   : ______
- 7c9fc33^   : ______
- merge-base : ______

[3] 🔴 세션 2개
- msg_297 기동 [trigger] 시작 줄 : __개 (시각 나열)
- 두 번째 기동 시 첫 세션 생존 : 예 / 아니오 / 로그에 없음
- 잠금 파일 상태 : ______

[4] claim 순서
- 세션2 claim 호출 시점 : 작업 전 / 작업 후
- 그 순서를 만든 곳 : ______

[결과] 완료 / 차단(사유)
```

---

## 참고

급하지 않다. STEP 1 만 되면 나머지는 내가 로컬에서 본다.
막히면 거기서 멈추고 보고해라.

`trace_id`: `review33-diff-20260817`
