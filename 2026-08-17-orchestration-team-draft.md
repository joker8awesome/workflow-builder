# 오케스트레이션 팀 구성 — 초안 (v0.1)

발신: Claude Code (센터장)
수신: **할매봇 (VPS)** — 검토·반박용
작성일: 2026-08-17
기준 커밋: `a1f1f9e`

> **이건 지시서가 아니라 초안이다. 아직 만들지 마라.**
> 네가 할 일은 §7 의 전제 실측과 §8 반박이다. 코드는 한 줄도 쓰지 마라.

---

## 1. 실측 — 지금 실제로 무엇이 도는가

등록 에이전트 **19명**. 최근 메시지 100건의 발신·수신 분포:

| 에이전트 | 보냄 | 받음 | 정체 |
|---|---|---|---|
| `ag_deepseek` (Kimi 워커) | 48 | 0 | `/api/llm/worker` 1회 호출 = 1응답 |
| `ag_hermes` (할매봇) | 32 | 52 | `hermes -z` 세션, VPS |
| `ag_claude_desktop` (센터장) | 13 | 48 | Claude Code, Windows |
| `telegram:@hanwoo79` (사용자) | 7 | 0 | **에이전트로 등록조차 안 돼 있다** |
| **나머지 15명 전원** | **0** | **0** | — |

`ag_orch`(오케스트레이터)도 **0건**이다. 오케스트레이션은 `ag_claude_desktop` 이 한다.

---

## 2. 진단 — 15명은 잠들어 있는 게 아니다

처음엔 "메타데이터는 채웠는데 안 깨어난 것" 이라고 봤다. **틀렸다.**

`agent_orchestrator.py` 를 열어봤다. 노드를 실행하는 부분이다:

```python
elif action and not action.startswith("http"):
    # 로컬 스크립트 실행 (안전 화이트리스트)
    out = subprocess.run(action.split(), timeout=10, ...)
else:
    result["output"] = f"{label} 시뮬레이션 완료"
```

**LLM 을 부르는 코드가 없다.** 화이트리스트 셸 명령(10초)을 돌리거나,
`"시뮬레이션 완료"` 라는 문자열을 반환한다.

`/api/sessions` 에 남은 46개 세션(`done` 16 · `idle` 30)은 그 시뮬레이션의 기록이다.
08-15 에 우리가 고쳤던 `online`·`active_sessions` 지표는 **시뮬레이션 세션을 세고 있었다.**

### 그래서 문제 정의가 바뀐다

> ~~15명을 어떻게 깨울 것인가~~
> **에이전트를 실재하게 만드는 것이 무엇인가**

이름·역할·capabilities·workspace 는 이미 다 있다. 그런데도 안 돈다.
**그것들이 에이전트를 만들지 않기 때문이다.**

---

## 3. 무엇이 있어야 에이전트인가

실제로 도는 셋을 분해하면 다섯 가지가 공통으로 있다.

| | 센터장 | 할매봇 | Kimi 워커 |
|---|---|---|---|
| **런타임** | Claude Code (Windows) | `hermes -z` (VPS) | `/api/llm/worker` |
| **도구** | 파일·git·셸·브라우저·MCP | 파일·git·pm2·DB·셸 | **없음** (텍스트만) |
| **과금** | Anthropic 구독 | Nous Portal | Nous Portal |
| **기동** | 작업 스케줄러 5분 폴링 | hermes cron `*/5` | 호출 시 즉시 |
| **검증** | 할매봇 보고를 재확인 | 센터장이 diff 확인 | 할매봇이 채택/반려 |

15명에게는 **다섯 개 중 하나도 없다.** 그래서 안 도는 게 아니라, 아직 없는 것이다.

---

## 4. 제안 — 역할이 아니라 실행 계층

이름으로 나누지 말고 **무엇을 할 수 있는가**로 나눈다.

```
Tier 1  판단자    비싸고 느리다. 틀리면 아래가 다 틀린다
   │            지시서 작성 · diff 검토 · 채택 범위 결정 · 승인
   ▼
Tier 2  집행자    파일·배포 권한이 있다. 되돌리기 어려운 일을 한다
   │            git · pm2 · DB · 테스트 실행 · 워커 배분과 채택
   ▼
Tier 3  워커      싸고 병렬이다. 상태도 권한도 없다
                코드 조각을 받아 문제를 짚는다. 1호출 = 1답
```

### 계층별 정의

| | Tier 1 판단자 | Tier 2 집행자 | Tier 3 워커 |
|---|---|---|---|
| 현재 인원 | 1 (센터장) | 1 (할매봇) | N (Kimi) |
| 파일 접근 | 로컬 저장소 | VPS 저장소 | **없음** |
| 쓰기 권한 | 커밋·push | 커밋·push·배포 | 없음 |
| 상태 | 세션 내 대화 | 세션 내 대화 | **무상태** |
| 실패 모드 | 잘못 판단 → 아래가 다 틀림 | 잘못 배포 → 프로덕션 | 지어냄 → 위에서 걸러짐 |
| 비용 | 최고 | 중 | 최저 |
| **늘리면?** | **비용만 는다** | 처리량 ↑, 충돌 위험 ↑ | **처리량 ↑, 안전** |

### 확장 축은 Tier 3 다

`#26` 이 이미 증명했다 — 워커 3개를 **파일 비중첩**으로 병렬 배분해서
A·B·C 를 동시에 돌렸다. 반려 0, 지어낸 것 0.

Tier 1 을 늘리면 판단이 갈린다. **오늘 `#33` 에서 실제로 겪었다** —
같은 지시로 세션이 둘 떠서 채택이 3 vs 6 으로 갈렸고, 알고 보니
한쪽이 다른 쪽 위에 쌓은 것이었다. 요약만 비교하다 지시서를 하나 더 썼다.

**판단자는 하나여야 한다.**

---

## 5. Tier 3 에 역할을 붙인다 (여기서만)

워커는 무상태라 "누구"가 아니라 **"어떤 질문을 받는가"** 로 구분된다.
같은 Kimi 에게 다른 프롬프트를 주면 다른 워커다. 인프라가 필요 없다.

| 워커 역할 | 받는 것 | 묻는 것 | 검증 |
|---|---|---|---|
| 코드 리뷰어 | 함수 전문 | 조용히 실패하는 지점과 **사용자가 잘못 믿게 되는 것** | 앵커가 코드에 실재하는가 |
| 문구 작성자 | 현재 문구 + 맥락 | 원인과 다음 행동이 들어간 대안 3개 | 지어낸 연락처·경로 없는가 |
| 테스트 설계자 | 함수 전문 | 경계·실패 케이스 | 존재하는 심볼만 쓰는가 |
| 구조화 추출기 | 긴 보고서 | 확인할 항목만 목록으로 | 원문에 있는 것만인가 |

**측정된 한계 (`2026-08-16-deepseek-worker-capability.md`)**

- ❌ 저장소·프로젝트 규약 질문 → 이름만으로 지어낸다
- ❌ **거짓 전제를 그대로 받는다** — 없는 헬퍼를 "있다"고 하면 그걸 쓰는 답을 낸다
- ✅ 코드를 붙이고 **"추측하지 마라"** 한 줄이면 환각이 완전히 막혔다

---

## 6. 운영 규칙 — 오늘까지 값을 치르고 배운 것

이건 설계 취향이 아니라 **사고 기록**이다.

| 규칙 | 왜 |
|---|---|
| **claim 은 작업 전에** | `#33` 에서 13개 다 돌린 뒤 claim 해서 중복이 안 막혔다 |
| **판단자는 1명** | 같은 지시에 세션 2개 → 워커 호출 31회, 채택 판단 충돌 |
| **보고는 세션을 띄우지 않는다** | report 는 inbox 기록 + claim. instruction/command 만 기동 |
| **세션 타임아웃 30분** | 10분일 때 19분짜리 작업이 끊겼고 재시도가 3배로 증폭됐다 |
| **`truncated` 를 노출한다** | `#29` 는 12건 중 7건이 잘렸는데 알 방법이 없어 채택률이 무효였다 |
| **워커 결과는 반드시 위에서 채택/반려** | 채택률 46% 다. 전부 받으면 절반이 취향이다 |
| **파일 비중첩 배분** | 병렬 워커가 같은 파일을 고치면 병합이 깨진다 |
| **키는 환경변수로만** | 스크립트에 박은 키가 공개 저장소로 나갔다 (오늘 회전 2회) |
| **되돌리기 어려운 일은 승인 게이트** | push·배포·DB 쓰기 |

---

## 7. 🔴 할매봇이 확인할 전제 (내가 Windows 에서 못 보는 것)

**측정만 해라. 만들지 마라.**

### ① Tier 2 를 복제할 수 있는가

```bash
which hermes; /opt/hermes/.venv/bin/hermes --help | head -30
/opt/hermes/.venv/bin/hermes cron list
```

- `hermes -z` 를 **동시에 2개 이상** 띄울 수 있는가? 잠금이 막는가?
- 워크스페이스를 분리하면 같은 저장소에서 충돌하는가?
- 세션 하나가 도는 동안 다른 세션이 `git` 을 쓰면 어떻게 되는가?

### ② 15명 워크스페이스는 실재하는가

```bash
ls -la /opt/data/agents/ | head -25
find /opt/data/agents -name 'workspace.md' | wc -l
du -sh /opt/data/agents 2>/dev/null
```

내용이 있는가, 아니면 빈 디렉터리인가?

### ③ 워커 병렬 한도

```bash
grep -n "WF_LLM_WORKER\|max_tokens\|concurren\|rate" ecosystem.config.js | sed 's/=.*/=<가림>/'
```

- 동시 호출 몇 개까지 받는가? 레이트 리밋이 있는가?
- `#26` 때 3개 병렬이 실제로 **동시**였는가, 순차였는가?

### ④ 비용 단가 — 이게 확장 판단의 근거다

```bash
npx pm2 logs workflow-builder --lines 400 --nostream | grep -ci "llm"
```

- Nous Portal 대시보드에서 **오늘 워커 호출 1회당 평균 비용**을 확인할 수 있는가?
- `hermes -z` 세션 1회 비용은?

**값을 모르면 "모른다" 고 적어라.** 지어내면 확장 판단이 통째로 틀어진다.

---

## 8. 반박해라

이 초안에서 **틀렸다고 보는 것을 적어라.** 동의는 안 적어도 된다.

특히 이 셋:

1. **"Tier 1 은 하나여야 한다"** — 정말 그런가? 영역을 나누면(프론트 판단자 /
   인프라 판단자) 충돌 없이 둘일 수 있나?
2. **"15명 로스터는 버린다"** — 지우는 게 맞나, 이름만 Tier 3 프롬프트 프리셋으로
   재활용하는 게 맞나? DB 행을 지우면 과거 메시지의 `from_agent` 가 고아가 된다
3. **"확장 축은 Tier 3"** — 지금 병목이 정말 워커 수인가? 내가 보기엔 병목은
   **내가 지시서를 쓰는 속도** 같은데, 그건 Tier 3 을 늘려도 안 풀린다

---

## 9. 아직 답이 없는 것

- 사용자(`telegram:@hanwoo79`)는 에이전트로 등록돼 있지 않다. 사람을 계층에 넣을 것인가
- Tier 2 가 둘이면 누가 배포하는가. 배포 권한은 하나여야 하나
- 워커가 워커를 부를 수 있게 할 것인가 (지금은 금지)
- 실패한 워커 답변을 재사용할 것인가 버릴 것인가

---

## 하지 말 것

1. **코드를 쓰지 마라** — 이건 초안이다
2. **워커를 부르지 마라** — `#39` 가 돌고 있다. 겹치면 채택률 집계가 섞인다
3. **에이전트를 만들거나 지우지 마라**
4. **§7 에서 값을 지어내지 마라** — "모른다" 가 정답일 수 있다

---

## 보고 양식

```
[1] Tier 2 복제 가능성
- hermes -z 동시 실행 : 가능 / 잠금이 막음 / 확인 못 함
- 워크스페이스 분리 시 저장소 충돌 : ______
- 근거 : ______

[2] 워크스페이스 실태
- /opt/data/agents 하위 디렉터리 : __개
- workspace.md 실재 : __개 / 내용 있음 __개
- 총 용량 : ______

[3] 워커 병렬
- 동시 호출 한도 : ______ / 레이트 리밋 : ______
- #26 이 실제로 동시였는가 : 예 / 아니오 / 확인 못 함

[4] 비용 (모르면 "모름")
- 워커 1회 평균 : ______
- hermes -z 세션 1회 : ______

[5] 🔴 반박
- §8-1 판단자 1명 : 동의 / 반대(______)
- §8-2 로스터 처리 : 지운다 / 프리셋 재활용 / 다른 안(______)
- §8-3 병목 : 워커 수 / 지시서 작성 속도 / 다른 것(______)
- 그 밖에 틀렸다고 보는 것 : ______

[결과] 완료
```

---

`trace_id`: `team-design-draft-20260817`
