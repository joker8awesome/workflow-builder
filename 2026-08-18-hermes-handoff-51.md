# 지시서 #51 — 이번 시즌 backfill: 결과(무료) + 과거배당 프로브(무료한도 보호)

작성: 센터장(Opus 4.8) · 2026-08-18 · 대상: 할매봇(ag_hermes)
trace_id: `season-backfill-20260818`

> 사용자 요청: 이번 시즌 MLB **종료 경기** 기준 데이터 수집, **무료 API 한도 절대 초과 금지.**
> 결과는 무료(MLB) → 지금 backfill. 배당은 무료 한도 미확인 → **대량 backfill 전 소량 프로브 먼저.**

## A. 결과 backfill — `fb_games` (MLB Stats API, 무료·무제한)
- #49 수집기(`collect-mlb`)를 **이번 시즌 전체 범위**로 1회 실행:
  `startDate=2026-03-01`(시즌 개막 근방) `endDate=오늘`, sportId=1.
- 종료 경기(status Final) 전부 `fb_games`에 UPSERT (game_pk·팀·날짜·home_score·away_score).
- **쿼터 걱정 없음**(MLB API 무료·키 불필요). 과도 폴링만 피하면 됨(백필은 날짜 청크로 나눠 호출).
- 합격: `SELECT count(*) FROM fb_games WHERE home_score IS NOT NULL` = 이번 시즌 종료 경기 수(대략 확인).

## B. 🔴 SGO 과거배당 프로브 — 읽기 전용, 최소 호출 (무료 한도 보호)
**대량으로 당기지 마라. 2~3회 호출로 "되는지·한도"만 확인.**
1. SGO가 **종료/과거 경기의 moneyline 배당**을 무료로 주는가? 어떤 쿼리로?
   - 과거 날짜/finished 필터 시도(예: `/v2/events?leagueID=MLB&startsBefore=<과거날짜>` 또는 문서의 finished 파라미터 — 실제 문서 확인).
2. 준다면 **한 이벤트에 배당이 단일값인가 시계열인가**(과거 변동 이력이 있나).
3. **무료 한도·현재 사용량**을 응답 헤더/계정에서 확인(남은 크레딧).
- ⚠️ **여기서 시즌 전체를 당기지 마라.** 한도·지원여부만 확인하고 보고. 대량 backfill 설계는 센터장이 예산 박아 별도 지시.

## C. 무료 한도 가드 (모든 SGO 호출 공통)
- 매 호출 전/후 **남은 크레딧 확인**, 임계(예: 남은 20%) 도달 시 **중단+보고**.
- 하드 상한: 이 지시 전체에서 SGO 호출 **≤ 10회**(프로브라서). 초과 예상되면 멈추고 보고.
- 키는 `WF_SGO_API_KEY` env에서만.

## 합격기준
- A: fb_games에 이번 시즌 종료 경기 적재(스코어 채워짐) 수 보고.
- B: SGO 과거배당 지원 여부(예/아니오·쿼리) + 단일값/시계열 + 무료 한도(총·남은) 보고. **SGO 호출 ≤10회.**
- 지어낸 값 0.

## 안 하는 것
- SGO 시즌 전체 대량 backfill(예산 확정 전). 무료 한도 초과. 키 노출. 야구 픽 테이블 쓰기.

## 보고 양식
```
[A 결과] fb_games 종료경기 __건 / 기간 __~__ / 호출수 __
[B 배당프로브] 과거배당 지원: 예(쿼리__)/아니오 / 단일값·시계열 / 무료한도 총__ 남은__
[C 가드] SGO 총 호출 __회(≤10) / 중단여부
[막힘] ______
```

`trace_id`: `season-backfill-20260818`
