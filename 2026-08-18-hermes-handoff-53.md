# 지시서 #53 — 2시즌 backfill (비용 0, 월 쿼터 하드캡)

작성: 센터장(Opus 4.8) · 2026-08-18 · 대상: 할매봇(ag_hermes)
trace_id: `two-season-backfill-20260818`

> 사용자 요청: **비용이 안 나면** 이번+직전 시즌 데이터 전부 긁어 fb_*에 합침.
> **무료 한도(2,500 엔티티/월) 절대 초과 금지 = 비용 0 보장.**

## 0. 🔴 전제 게이트 (#52 프로브 결과)
- **SGO 무료가 과거(완료) 경기 moneyline 배당을 주는가?** (#52 §1 프로브)
  - **아니면 / 유료면 → 배당 backfill 중단 + 보고.** 절대 유료로 진행하지 마라. (결과 backfill(§1)은 무료라 진행)
- SGO 과거배당 **깊이** 확인: 2025 시즌(직전)까지 닿는가? 안 닿으면 그 부분 "불가"로 보고.

## 1. 결과 backfill — 양 시즌 (MLB Stats API, 무료·무제한)
- `collect-mlb-backfill`을 **직전 시즌(2025 전체)** 범위로 실행 → `fb_games` UPSERT (이번 시즌은 이미 있음).
- 쿼터 무관(MLB 무료). 날짜 청크로 나눠 과도 폴링만 피함.
- 합격: `SELECT count(*), min(game_date), max(game_date) FROM fb_games` — 2시즌 커버 확인.

## 2. 배당 backfill — 이번 시즌만 이번 달 (SGO, 하드캡)
- 🔴 **하드 쿼터캡**: 이번 실행에서 SGO 엔티티 사용 **≤ 2,400**(2,500에서 안전마진 100). 매 호출 후 누적 카운트, **2,400 도달 시 즉시 중단 + 보고.** 절대 초과 금지.
- 대상: **이번 시즌 완료 경기**의 과거 marketline 배당(가능하면 마감값) → `fb_odds_snapshots` (sgo_event_id·game_date·팀·collected_at 저장).
- fb_games 조인: `(game_date+홈+원정)` (#50에서 팀명 표기 일치 확인됨).
- ⚠️ **직전 시즌 배당은 이번 달에 하지 마라** — 합치면 월 한도 초과. **다음 달(쿼터 리셋) 별도 지시**로.
- 과거배당이 **단일값(마감)**일 가능성 높음 — T-6h 변동율은 이 backfill로 못 만든다(수준 기반 공식용). 그대로 저장.

## 3. cron
- **등록하지 마라.** 사용자 지시 없으면 수집 중단(1회 backfill만).

## 합격기준
```
[게이트] SGO 과거배당 무료 지원: 예/아니오 / 깊이 2025까지: 예/아니오
[결과] fb_games 2시즌 __건 (이번__ 직전__) / 스코어 __
[배당] 이번시즌 SGO 엔티티 사용 __(≤2400) / fb_odds 추가 __행 / (배당+결과) 겹치는 경기 __
[다음달예정] 직전시즌 배당 ~__엔티티 (9월 쿼터)
[비용] 0 확인 (유료 호출 0)
[막힘] ______
```

## 안 하는 것
- 유료 발생(무료 지원 안 되면 중단). 2,400 엔티티 초과. 두 시즌 배당 동시(월 한도 초과). cron 등록. 키 노출. 야구 픽 테이블 쓰기.

`trace_id`: `two-season-backfill-20260818`
