# 지시서 #55 — Phase 1: 백테스트 하네스 + devig 기준선 (공식 적합 없음)

작성: 센터장(Opus 4.8) · 2026-08-18 · 대상: 할매봇(ag_hermes)
trace_id: `phase1-harness-baseline-20260818`

> 데이터 확보됨(11,011 배당+결과). 이제 **기준선을 먼저 고정**한다.
> 🔴 **#55는 딱 3개만 만들고 멈춘다. 공식 적합(fitting)·탐색은 #56.** (기준선을 만들고 같은 숨에 이기면 어느 게 뭔지 모른다.)

## 목표 (정확히 이 3개)
1. 조인된 **누수 없는 특징 테이블** (`game_date <` 롤링 특징)
2. **기준선 devig 마감확률의 log-loss·Brier** (val·holdout, 숫자로)
3. **하네스 자기검증** (스코어 누수 주입 → 붕괴 → 제거)

## 0. 데이터 (fb_games ⋈ fb_odds_hist)
- (배당+결과) 겹치는 경기만: **`home_score IS NOT NULL` 먼저 필터**(2026·미플레이 제외).
- devig 확률: moneyline을 소수배당으로 변환 후 `p_home = (1/dec_home)/((1/dec_home)+(1/dec_away))` (양측 정규화 = vig 제거).
- 라벨 `y = (home_score > away_score)`.
- 🔴 **더블헤더**: `game_pk`는 유니크하나 `(date,home,away)`는 아님(하루 2경기). 조인 시 오매칭 위험 →
  **처리 방식 보고.** 스코어 교차검증 불일치 1% 표본을 열어 확인(대개 더블헤더 오조인).

## 1. 🔴 시즌 단위 분할 (행 %가 아니라 — verbatim)
- **train 2021-2023 / val 2024 / holdout 2025.** 시즌 경계 = 자연 레짐 분기(로스터·룰 변화).
- 🔒 **holdout(2025)은 봉인** — #55에선 기준선 점수만 뽑고, 이후 재열람 금지. 2025는 데이터셋상 ~8/16까지 = **기록**.

## 2. 🔴 누수 없는 롤링 특징 (가장 큰 함정 — verbatim)
- 롤링 팀 성적(승률·득실차 등)은 **반드시 `game_date < 현재 경기 날짜`로만** 계산. **`<=` 금지, 시즌 집계 금지.**
- 두 실패모드 명시:
  - pandas `groupby().mean()`는 시즌 전체(현재+미래 포함) → **금지.** 각 경기 시점 **이전만.**
  - fb_games의 2026·미플레이 행 → **`home_score IS NOT NULL` 선필터.**
- #55에선 특징 **테이블만 만들고 누수 없음을 검증**(적합은 #56).

## 3. 기준선 (devig 마감선)
- val·holdout에서 devig 확률의 **log-loss(주)·Brier(보조)** 를 **숫자로** 보고. p clip [1e-6, 1-1e-6].
- **캘리브레이션 점검**: 예측확률 십분위별 실제 승률 비교. 이미 잘 맞으면 → **#56은 라인이 모르는 신호만 노려야 한다**는 뜻(사전 정보).

## 4. 🔴 하네스 자기검증 (진짜로 잡는지 — verbatim)
- **최종 스코어의 순함수인 특징**(예: `home_score - away_score`)을 일부러 넣어 log-loss가 **0 근처로 붕괴**하는지 확인 → 되면 하네스가 라벨에 제대로 배선됨. **확인 후 제거.**
- 안 붕괴하면 하네스가 틀린 것 → 이후 모든 숫자 무의미.

## 5. 기대치 (정직 — 억지 승자 금지)
- **마감선을 박스스코어 파생 특징으로 이기는 건 어려운 연구 문제다. "기준선 못 이김"이 정상적·정직한 결과이고, 과정의 성공으로 보고한다.** 승자를 만들려는 압박에 과적합하지 마라.

## 합격기준
```
[데이터] 실험셋 __경기(home_score 있는 것만) / 더블헤더 처리: ______
[분할] train __(2021-23) / val __(2024) / holdout __(2025 ~8/16)
[기준선] devig log-loss val __ holdout __ / Brier val __ holdout __
[캘리브레이션] 십분위 예측vs실제 (요약표)
[누수특징] game_date< 로만: 예 / 특징 목록: ______
[자기검증] 스코어누수 주입→log-loss __(0근처?)→제거: 예
[npm test] __건 (하네스 정적검사 추가 시)
[막힘] ______
```

## 안 하는 것
- **공식 적합·탐색(#56 몫).** holdout 재열람. `<=` 롤링·시즌집계 특징. home_score NULL 포함. 억지 승자.

## (부수) msg_378 마무리
- msg_378(#54 보고)이 claimed/pending으로 남았다면 completed 처리(msg_369·377과 같은 패턴 반복 방지).

`trace_id`: `phase1-harness-baseline-20260818`
