# 자판기 → 코디네이터 지침서 · 작업 1~4 완료 보고 및 조치 요청

> 발신: SNS자판기(Content QA v6 / sns-jping) 관리 세션 · 2026-08-22
> 수신: SNS 워크스페이스 코디네이터 (Instagram 세션 겸임)
> 근거 지침서: `vending_session.md` (v2, 2026-08-21) 요청 작업 1·2·3·4
> 대상 정본: `D:\naver\SNS자판기_소스` = `github.com/joker8awesome/sns-jping` (private)
> **최종 상태: main `0a272a6` · 골든 회귀 70/70 PASS · FAIL 0 · 라이브 앱 200**

---

## 0. 한 줄 요약

**작업 1·2·4 완료, 작업 3은 "등록하지 않음"으로 결론.** 다만 작업 1·2는 "이미 구현돼 있다"가 아니라 **"구현은 돼 있는데 실사용에서 도달할 수 없는 상태"** 였고, 그 구멍 두 개를 막은 것이 이번 작업의 실질입니다. 코디네이터 조치 대기 **5건**(§3), 그중 P0 2건.

---

## 1. 완료 현황

| 작업 | 상태 | 근거 |
|---|---|---|
| 1. 임계값 프로필 3종 | ✅ **실사용 가능** | 정의 `702184a` + **설정 UI `6f05bad`** |
| 2. 커스텀 룰 4종 + 골든 매트릭스 | ✅ **4/4 룰 4칸 충족** | 룰 `702184a` + negation 마감 `6f05bad`·`0a272a6` |
| 3. 정책 스냅샷 소스 등록 | ⛔ **등록 안 함 (결론)** | §2-3 |
| 4. 검수 리포트 회신 포맷 | ✅ **이미 충족, 변경 없음** | `app.py:287` |

### 1-1. 작업 1 — 프로필이 켜지지 않던 문제

`config.json.threshold_profiles`에 6종(default·commercial·informational·**ig-sports·persona-threads·persona-x**)이 정의돼 있었고, 읽기 배선도 온전했습니다:

```
사이드바 계정 selectbox → account_id → full_qa() → account_thresholds() → analyze(thresholds=)
```

**그런데 `accounts.threshold_profile`에 값을 쓰는 UI가 어디에도 없었습니다.** `UPDATE accounts` 문 자체가 코드에 없었고, 계정 추가 폼도 이 컬럼을 받지 않았습니다. 라이브 DB 4계정 전부 `default` 고정.

→ **persona-x 전략 게이트(본문 URL REVIEW 승격)가 실사용에서 단 한 번도 발동할 수 없는 상태였습니다.** 회귀 테스트는 `analyze(thresholds=…)`로 값을 직접 주입하므로 이 구멍을 잡지 못했습니다.

**조치:** 관리>Accounts 에 프로필 선택(계정 추가 시) + 기존 계정 프로필 변경(`UPDATE accounts`) 추가. 선택한 프로필의 임계값·전략 게이트를 화면에 캡션으로 노출하고, 변경 이력은 `ui_events`에 기록합니다.

**E2E 검증(DB 복사본):**

```
x 계정  default → persona-x
  임계값   25/55/70 → 20/50/68
  전략     [] → ['body_url_review']
  본문     "오늘 운동 루틴 정리했어요 https://example.com/routine 참고하세요"
  verdict  PASS → REVIEW
  total    0.0 → 0.0   ← 수치 불변(전략 게이트가 점수를 오염시키지 않음)
```

**곁가지 수정:** 배치 검사(U1)가 계정 프로필을 무시하고 있었습니다 → 같은 프로필을 쓰도록 수정. 같은 본문이 단건/배치에서 다른 verdict를 내지 않습니다. (앱 내장 Golden>Regression 은 골든 label이 default 기준으로 시드돼 있어 의도적으로 default 유지 — 캡션에 명시했습니다.)

### 1-2. 작업 2 — `persona_suggestive`만 negation 칸이 비어 있던 문제

지침서 요구는 룰당 **positive·benign·negation·scope 4종**입니다. 4개 중 셋은 갖췄고 하나가 비어 있었습니다.

| 룰 | `negation_aware` (수정 전) | 골든 negation (수정 전) |
|---|---|---|
| persona_meetup_dm | `true` | PM3 ✅ |
| persona_medical_claim | `true` | PC3 ✅ |
| persona_false_affiliation | `true` | PF3 ✅ |
| **persona_suggestive** | **없음** | **없음** |

**실측 피해:** 부정 문맥(`'노출 컷' 같은 표현 하면 안 됩니다`)에서도 **distribution +20 / total 4.40을 그대로 가산**하고 있었습니다. 형제 룰 3종은 0입니다.

→ `negation_aware: true` 추가로 해소. 골든 PS5(negation) + PS6(적색등: 실위반은 여전히 발동) 고정.

### 1-3. ★ 부수 발견 — 골든 하네스가 이 결함을 구조적으로 못 잡습니다

골든 러너의 "발동" 판정은 `fired = [e for e in evidence if not e["negated"]]` 입니다. 그런데 `negated` 플래그는 **`negation_aware`와 무관하게** 계산됩니다. 즉 `negation_aware`가 빠진 룰은 **부정 문맥에서 축 점수를 그대로 더하면서도 골든은 통과**합니다. 68/68 PASS가 이 결함 위에서 나오고 있었습니다.

→ 축 점수·total이 실제로 0인지 직접 보는 **`persona_negation_checks()`** 를 신설해 persona 룰 4종을 고정하고, `main()` 종료코드에 반영했습니다.

### 1-4. 작업 4 — 검수 리포트 회신 포맷

**이미 충족, 변경 없음.** MD 리포트(`app.py:287`)에 요구 필드 전부 포함:
`platform` · `verdict` · `total` · `confidence` · 걸린 `rule_id` 목록 · `evidence` 문장 원문 · **축별 점수**

---

## 2. 정직 고지 — 한 번 잘못 갔다가 되돌린 것

작업 2 진행 중 문구 변형 대응을 위해 `negation_patterns`에 2건을 보강했다가 **되돌렸습니다(`0a272a6`)**.

- `negation_patterns`는 **전역 목록**이고 `negation_aware` 규칙 **9종이 공유**합니다.
- E2(부정 문맥 ±1 문장 창)와 곱해지면, 본문 뒤에 금지문 한 줄만 붙여도 **이웃 문장의 규칙이 억제**됩니다.
- 이웃 억제 자체는 E2의 기존 성질입니다. 문제는 추가한 패턴이 **새 문구 2종에 대해 그 사정권을 대폭 넓힌 것**입니다:

| 접미문장 | 수정 전 | 확대 후 | 되돌린 뒤 |
|---|---|---|---|
| `~같은 말 하면 안 돼요` (기존 사정권) | 7/8 | 7/8 | 7/8 |
| `이런 문구 쓰면 안 됩니다` | 1/8 | **7/8** | 1/8 |
| `이런 내용 금지` | 1/8 | **7/8** | 1/8 |

*(positive 샘플 8건 × 접미문 실측. 확대 사정권 안에 `naver_account_trade`·`illegal_promotion` 등 **critical:true BLOCK 규칙**이 포함됨.)*

- **되돌려서 잃은 것:** 자체 작성한 문구 변형 골든 2종뿐입니다. 운영자가 실제로 쓸 카논 형태(`'…' 같은 표현 하면 안 됩니다`)는 기존 패턴이 이미 잡고 PS5로 고정돼 있습니다.
- **`persona_suggestive`의 실제 결함은 해소 상태 유지** — 그건 단일 규칙 범위 수정이라 되돌리지 않았습니다.
- **재발 방지 핀:** `persona_negation_checks()`에 적색등 추가 — critical 3종 × 금지문 3종 = 9쌍이 접미 금지문에 억제되지 않는지 검증. 느슨한 패턴 재투입 시 `illegal_promotion`·`guarantee_claims`가 실제로 FAIL 나는 것 확인 완료.

> 📌 **코디네이터·후속 세션 참고:** 앞으로 negation 오탐 민원이 와도 **`negation_patterns` 전역 목록을 넓히는 방식은 쓰지 마세요.** 개별 룰의 패턴을 정밀화하거나, 해당 룰에만 붙는 예외를 쓰는 방향이 맞습니다.

---

## 2-3. 작업 3 — 정책 스냅샷 소스 등록: **등록하지 않음 (결론)**

대상: `_shared\threads_algorithm_prd.html`, `_shared\x_strategy_advanced.md`

**기술적으로는 가능합니다.** `refresh_policy_snapshots`가 `urllib.request.urlopen`을 쓰므로 `file:///C:/sns/_shared/x_strategy_advanced.md` 형태로 넣으면 **코드 변경 없이** 스냅샷·diff가 돕니다(실측 확인).

**그럼에도 등록을 권하지 않는 이유 3가지:**

1. **근거등급 오염.** 현재 `rules.json.sources` 15건은 전부 **플랫폼 공식 정책 URL**이고, 이게 룰의 `OFFICIAL` 근거등급을 떠받칩니다. 코디네이터 2차 해석 문서를 같은 채널에 넣으면 "공식 정책이 바뀌었다"와 "우리 해석 문서가 바뀌었다"가 구분되지 않습니다.
2. **이미 내린 결정과 정면 모순.** `x_strategy_advanced.md`의 "X 링크 페널티 -50~94%" 류는 자판기가 **반증완료로 분류해 전역 룰 금지**한 항목입니다(공개 랭킹 param에서 `open_link`는 오히려 +0.2, 페널티 규칙 부재). 그래서 persona-x 본문 URL을 정책 룰이 아닌 **전략 게이트**로 분리한 것입니다. 이걸 정책 감시 소스로 승격하면 그 결정을 스스로 뒤집는 셈입니다.
3. 두 문서 모두 아직 `_shared/`에 없습니다.

**대안 제안:** 별도 `strategy_sources` 채널을 만들고 근거등급을 `STRATEGY`로 분리하면, 해석 문서 변경 감지는 하면서 OFFICIAL 등급은 지킬 수 있습니다. 전략 게이트가 이미 쓰는 패턴이라 구조도 일관됩니다. **승인하시면 착수합니다** (§3-4).

---

## 3. 코디네이터 조치 요청

### 3-1. 【P0】 `_shared\brand_persona.md` 배치

**이것이 남은 유일한 차단 항목입니다.** `persona_suggestive`의 현재 패턴은 지침서 초안 그대로이며 **잠정값**입니다:

```
속옷|란제리
(?:수영복|비키니)\s?화보
(?:과다|전신)\s?노출|노출\s?(?:컷|샷)
```

- **필요한 것:** 2부 수위 가이드 — 허용 경계(룩북·몸매 강조 옷차림·섹시한 일상 포즈 = SFW)와 금지 경계(속옷·란제리·과다 노출·화보체 수영복)를 **어휘 수준으로** 구분할 수 있는 서술.
- **수행 조건:** 자판기 세션은 이 문서를 **작성하지 않습니다**(코디네이터 소관). `_shared/`는 읽기 전용으로 취급 중입니다.
- **완료 조건:** 문서 배치 후 `_inbox\vending.md`로 통보 → 자판기 세션이 패턴 재튜닝 + benign/negation 골든 보강 + 회귀 재실행 후 회신.

### 3-2. 【P0】 임계값 프로필 배정 확정

UI는 넣었지만 **라이브 DB 계정 4개는 전부 `default` 그대로 뒀습니다.** 계정이 아직 플랫폼별 "기본 계정" 플레이스홀더라 임의 배정하지 않았습니다.

- **권고 매핑:** instagram → `ig-sports` · threads → `persona-threads` · x → `persona-x` · naver_blog → `default`(비사용)
- **결정 필요:** ① 위 매핑대로 갈지 ② 서하린용 계정을 플레이스홀더 대신 **실명 계정으로 새로 만들지**(예: `서하린-threads`, `서하린-x`)
- **수행:** 관리>Accounts 에서 클릭 한 번입니다. 코디네이터가 지시하면 자판기 세션이 대신 적용합니다.
- **완료 조건:** 배정 후 `persona-x` 계정에서 본문 URL 포함 초안 1건 검사 → verdict REVIEW + `strategy_flags=['body_url_review']` 확인.

> **Phase 3 전환 프로토콜(지침서 §1):** Phase 3 진입 시 persona 프로필을 commercial 계열로 전환합니다. **코디네이터 통보가 트리거**이며 자판기가 자동 판단하지 않습니다.

### 3-3. 【P1】 파이프라인 담당에게 — `MEDICAL_CLAIM` 동기화 지시

`D:\로컬LLM\crawl_sns_rss.py`의 `MEDICAL_CLAIM`과 정본 `rules.json.persona_medical_claim`은 "의도적 복제본, 함께 갱신" 계약인데 **실제로는 6갈래 중 4갈래가 어긋나 있습니다**:

| 갈래 | 파이프라인 필터 | 정본 룰 |
|---|---|---|
| 무조건 | `무조건\s?(빠\|뻐)` | `무조건\s?(?:빠\|뻐\|감량)` ← **감량** 추가 |
| 100% | `100%\s?감량` | `100\s?%\s?감량` ← **% 앞 공백 허용** |
| 부작용 | `부작용\s?없` | `부작용\s?(?:없\|제로)` ← **제로** 추가 |
| 살/지방 | `(살\|지방)\s?무조건` | `(?:살\|지방)\s?(?:무조건\|확실히)\s?(?:빠\|감)` ← **확실히** 추가, 단 동사 필수 |

- **영향:** 파이프라인 필터가 대체로 더 좁아 **자판기가 잡을 표현이 소재 수집 단계는 통과**합니다.
- **방향:** 정본 기준으로 파이프라인을 맞추는 것이 맞습니다(반대 아님 — 정본이 정책 근거를 들고 있음).
- **수행자:** 파이프라인 담당. 자판기 범위 밖이라 손대지 않았습니다.
- **완료 조건:** 두 패턴이 동일해진 뒤 `_inbox\vending.md`로 통보 → 자판기가 대조 확인 회신.

### 3-4. 【P1】 작업 3 결론 승인 / `strategy_sources` 착수 여부

§2-3의 "등록하지 않음" 결론을 승인하시는지, 아니면 대안(`strategy_sources` 채널 + `STRATEGY` 근거등급 분리)을 착수할지 지시해 주세요. **착수 지시가 없으면 현행 유지**(등록 안 함)로 갑니다.

### 3-5. 【P1】 죽은 사본 폐기 확정

`D:\SNS자판기\SNS자판기_소스`는 **git repo가 아닌 8/20 zip 압축해제본**입니다(`6.0.5-branding`, 규칙 10종, 프로필 키 `profiles`, `regression/run_regression.py`). 2026-08-22 01:09~01:12에 **다른 세션이 여기에 Part 2를 독립 재구현**해 이중 구현이 생겼습니다.

- **정본은 `D:\naver\SNS자판기_소스`** 하나뿐입니다. 판별법: `git remote -v`가 `sns-jping`을 가리키는지.
- **요청:** 죽은 사본 폐기(또는 `_deprecated_` 접두 리네임)를 확정해 주세요. 그대로 두면 다음 세션이 또 그쪽에 작업합니다.
- 건질 것: 그쪽 `regression/persona_golden.json` 16종 중 정본 골든 70에 없는 케이스만 이관 검토. (MD 리포트 필드는 정본이 축별점수까지 이미 포함 — 이관 불필요.)

---

## 4. 자판기 세션이 계속 들고 갈 것 (코디네이터 조치 불필요)

지침서 작업 1~4 범위 밖이지만 자체 감사(`D:\Comment_Center\2026-08-22-snsjping-completion-audit.md`)에서 나온 항목으로, 자판기 세션이 후속 처리합니다. 코디네이터 결정은 필요 없습니다.

- **SPEC.md 부분 갱신 누락** — §7 규칙 카탈로그에 persona 4종이 없고(16 규칙 표기, 실제 20), §15 테스트 수·§16 커밋 수가 낡음.
- **D2(규칙 신뢰도→confidence) 죽은 함수** — `database.rule_reliability`가 `app.py` import 목록에 없어 생산부 호출자 0. "데이터 성숙 시 활성화" 결정만 기록되고 활성화 경로는 만들어진 적 없음.

---

## 5. 검증 재현 방법

```
cd D:\naver\SNS자판기_소스
git log --oneline -1                 # 0a272a6 이어야 함
git remote -v                        # sns-jping 이어야 함 (죽은 사본 판별)
"D:\naver\SNS자판기_실행본\python\python.exe" -s -E tests/golden_regression.py
```

기대 출력: `규칙 회귀: 70/70 PASS (100.0%)` + `실사용 수정 / v6.0.6 / v6.1.0 / persona-x 전략게이트 / persona negation_aware 축-레벨` 전 섹션 PASS, 종료코드 0.

**커밋 계보(이번 작업 구간):** `702184a`(룰 4종·프로필 3종·리포트 필드) → `9d70d42`(persona-x 전략 게이트) → `6f05bad`(프로필 설정 UI·negation_aware·배치 thresholds·하네스 보완) → `0a272a6`(전역 negation 패턴 되돌림·과확대 방지 적색등)

**동기화 상태:** `_소스` = `origin/main` = `_실행본\app` (코드 5파일 md5 일치), 라이브 앱 `localhost:8501` HTTP 200.

---

## 6. 비범위 (변동 없음)

- **탐지 회피 기법 개발 금지** — 자판기는 정책 준수 도구로 유지합니다. 이번 작업에 회피 기능은 없습니다(persona 룰 4종은 전부 정책 준수화 방향: 사칭·스캠 오인·선정성·오정보 flag).
- 네이버 블로그 기능은 현 워크스페이스 비사용 — 기능만 유지.
- 재작성은 플랫폼 세션 담당. 자판기 AI 재작성 4종은 폴백으로만 유지.
